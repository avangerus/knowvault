openapi: 3.1.1
info:
  title: KnowVault Internal API
  version: 0.0.0-stage1
  description: >-
    Authenticated on-edge KnowVault API for workspace administration, source ingestion, keyword and
    semantic retrieval, evidence reads, agent access, audit metadata and optional model answers. The
    server composes these routes behind startup, tenant-security and workspace-authorization gates;
    model and connector availability still depends on configured capabilities. The info.version,
    x-implementation-status and x-knowvault-ui-surfaces status values include retained historical
    annotations and are not current deployment-readiness or pilot-acceptance guarantees. See [pilot
    status](../docs/PILOT-STATUS.md) for the qualified release scope; a described endpoint or
    optional capability does not by itself establish production readiness.
  x-implementation-status: HANDLER_READY_NOT_COMPOSED
servers:
  - url: /api/v1
tags:
  - name: System
    description: "Process diagnostics without tenant or source content."
  - name: Workspace
    description: >-
      Protected workspace control plane composed in the server. JSON request objects are limited to
      32768 bytes and decoded strictly: duplicate or unknown members, case aliases, invalid UTF-8,
      non-object bodies, and trailing JSON values are rejected.
    x-implementation-status: LIVE
  - name: Search
    description: >-
      Workspace retrieval and persisted Question Runs with EXTRACTIVE, TOOL_LOOP and compatibility
      GENERATIVE modes. Runs record their workspace revision, answer mode and evidence references;
      the selected mode determines verification. Model profiles are workspace-scoped, and valid
      citations alone do not establish answer completeness or correctness.
    x-implementation-status: LIVE_EXTRACTIVE
  - name: Sources
    description: >-
      Source registration, workspace binding and activation, including scheduled refresh for mounted
      folders and prepared PostgreSQL projections. Git registration remains declarative; a listed
      source type does not establish a configured or healthy connector.
    x-implementation-stage: STAGE_2_AND_5
  - name: Access
    description: >-
      Implemented workspace-scoped agent access-code issuance, listing and revocation, plus
      managed-source confirmation operations. Each operation applies its current owner or
      delegated-authority checks; access to these controls does not automatically grant
      source-content access.
    x-implementation-stage: STAGE_1
  - name: Audit
    description: >-
      Implemented, paginated workspace audit-metadata reads with current tenant, workspace and
      audit.read_metadata checks. The response excludes document bodies, model answers and SQL;
      audit access does not automatically grant source-content access.
    x-implementation-stage: STAGE_1
x-knowvault-ui-surfaces:
  - id: workspace
    label: "Workspaces"
    api_tag: Workspace
    status: LIVE
  - id: search
    label: "Search"
    api_tag: Search
    status: LIVE_EXTRACTIVE
  - id: sources
    label: "Sources"
    api_tag: Sources
    status: LIVE
  - id: access
    label: "Access"
    api_tag: Access
    status: RESERVED_NO_HANDLER
  - id: audit
    label: "Activity log"
    api_tag: Audit
    status: RESERVED_NO_HANDLER
paths:
  /system/health:
    get:
      operationId: getSystemHealth
      tags: [System]
      summary: "Process health"
      description: "Does not check readiness of future business modules or expose their data."
      responses:
        "200":
          description: "The process is running."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
  /system/build-info:
    get:
      operationId: getBuildInfo
      tags: [System]
      summary: "Build identity"
      description: "Returns only versioned build metadata, without configuration or secrets."
      responses:
        "200":
          description: "Metadata for the exact build."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BuildInfoResponse"

  /system/capabilities:
    get:
      operationId: getSystemCapabilities
      tags: [System]
      summary: "Actual process capabilities and readiness"
      description: >-
        Returns only a content-free snapshot of server capabilities. READY means that the
        corresponding server-owned adapter is composed and has passed the startup gate;
        PARTIAL/BLOCKED/UNAVAILABLE retain the external blocker. The endpoint exposes no tenant,
        source, endpoint, secret, or payload data and does not constitute Enterprise release
        acceptance.
      x-implementation-status: LIVE
      responses:
        "200":
          description: "Capability snapshot without configuration or tenant data."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SystemCapabilitiesResponse"

  /session/csrf:
    get:
      operationId: getCSRFToken
      tags: [Workspace]
      summary: "Get the current session's CSRF proof"
      description: >-
        Requires one valid transport credential: a session cookie for the browser, or the same
        server-issued session token in Authorization: Bearer for API/MCP. Returns only a
        tenant-bound derived proof, never the raw session cookie. The response always uses no-store.
      x-implementation-status: HANDLER_READY_NOT_COMPOSED
      security:
        - SessionCookie: []
        - BearerSession: []
      responses:
        "200":
          $ref: "#/components/responses/CSRFResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces:
    get:
      operationId: listWorkspaces
      tags: [Workspace]
      summary: "List visible workspaces"
      description: "Query parameters are not supported."
      x-implementation-status: HANDLER_READY_NOT_COMPOSED
      security:
        - SessionCookie: []
        - BearerSession: []
      responses:
        "200":
          $ref: "#/components/responses/WorkspaceListResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
    post:
      operationId: createWorkspace
      tags: [Workspace]
      summary: "Create a workspace"
      description: >-
        The only mutation without If-Match. Idempotency-Key is required; its raw value is neither
        stored in audit nor returned.
      x-implementation-status: HANDLER_READY_NOT_COMPOSED
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WorkspaceMetadataInput"
      responses:
        "200":
          $ref: "#/components/responses/WorkspaceResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CreateForbiddenOrCSRFRejected"
        "409":
          $ref: "#/components/responses/IdempotencyConflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
    get:
      operationId: getWorkspace
      tags: [Workspace]
      summary: "Current workspace configuration"
      x-implementation-status: HANDLER_READY_NOT_COMPOSED
      security:
        - SessionCookie: []
      responses:
        "200":
          $ref: "#/components/responses/WorkspaceResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
    put:
      operationId: replaceWorkspaceMetadata
      tags: [Workspace]
      summary: "Completely replace workspace metadata"
      x-implementation-status: HANDLER_READY_NOT_COMPOSED
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WorkspaceMetadataInput"
      responses:
        "200":
          $ref: "#/components/responses/WorkspaceResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/IdempotencyConflict"
        "412":
          $ref: "#/components/responses/RevisionConflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "428":
          $ref: "#/components/responses/PreconditionRequired"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}:archive:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
    post:
      operationId: archiveWorkspace
      tags: [Workspace]
      summary: "Transition the workspace to ARCHIVED"
      description: "No body is allowed; even `{}` is an invalid request."
      x-implementation-status: HANDLER_READY_NOT_COMPOSED
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
        - $ref: "#/components/parameters/IfMatch"
      responses:
        "200":
          $ref: "#/components/responses/WorkspaceResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/IdempotencyConflict"
        "412":
          $ref: "#/components/responses/RevisionConflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "428":
          $ref: "#/components/responses/PreconditionRequired"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/members:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
    post:
      operationId: addWorkspaceMember
      tags: [Workspace]
      summary: "Add a member"
      x-implementation-status: HANDLER_READY_NOT_COMPOSED
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddMemberInput"
      responses:
        "200":
          $ref: "#/components/responses/WorkspaceResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/IdempotencyConflict"
        "412":
          $ref: "#/components/responses/RevisionConflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "428":
          $ref: "#/components/responses/PreconditionRequired"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/member-candidates:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
    get:
      operationId: listWorkspaceMemberCandidates
      tags: [Workspace]
      summary: "Find workspace membership candidates (P10a)"
      description: >-
        Reads only ACTIVE USER principals in the caller's organization and returns a bounded
        deterministic list of candidates (principal_id, display_name), with already_member
        indicating existing membership so the UI does not guess identifiers or permit duplicate
        additions. Available only to callers who manage this workspace's members (OWNER or MANAGER
        of an active workspace, using the same gate as AddMember). The search does not mutate state
        or return credentials, email, external identity links, inactive/service/group principals, or
        other tenants. q is matched literally as a case-insensitive substring of id or display_name,
        with LIKE metacharacters escaped: 2..100 Unicode characters after trimming, at most 20
        results, and an accurate truncated flag.
      parameters:
        - name: q
          in: query
          required: true
          description: >-
            Literal search: a case-insensitive substring of id or display_name, with LIKE
            metacharacters escaped. Requires 2..100 Unicode characters after trimming. Exactly one q
            parameter; duplicates or an empty value produce REQUEST_INVALID.
          schema:
            type: string
            minLength: 2
            maxLength: 100
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      responses:
        "200":
          description: "Bounded candidate list with an accurate truncation flag."
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [candidates, truncated]
                properties:
                  candidates:
                    type: array
                    items:
                      $ref: "#/components/schemas/MemberCandidate"
                  truncated:
                    type: boolean
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/members/{principal_id}:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
      - $ref: "#/components/parameters/PrincipalID"
    put:
      operationId: changeWorkspaceMemberRole
      tags: [Workspace]
      summary: "Change a member's role"
      x-implementation-status: HANDLER_READY_NOT_COMPOSED
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MemberRoleInput"
      responses:
        "200":
          $ref: "#/components/responses/WorkspaceResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/IdempotencyConflict"
        "412":
          $ref: "#/components/responses/RevisionConflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "428":
          $ref: "#/components/responses/PreconditionRequired"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
    delete:
      operationId: removeWorkspaceMember
      tags: [Workspace]
      summary: "Remove a member"
      description: "No body is allowed; even `{}` is an invalid request."
      x-implementation-status: HANDLER_READY_NOT_COMPOSED
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
        - $ref: "#/components/parameters/IfMatch"
      responses:
        "200":
          $ref: "#/components/responses/WorkspaceResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/IdempotencyConflict"
        "412":
          $ref: "#/components/responses/RevisionConflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "428":
          $ref: "#/components/responses/PreconditionRequired"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}:transfer-ownership:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
    post:
      operationId: transferWorkspaceOwnership
      tags: [Workspace]
      summary: "Transfer ownership to an existing member"
      x-implementation-status: HANDLER_READY_NOT_COMPOSED
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TransferOwnershipInput"
      responses:
        "200":
          $ref: "#/components/responses/WorkspaceResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/IdempotencyConflict"
        "412":
          $ref: "#/components/responses/RevisionConflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "428":
          $ref: "#/components/responses/PreconditionRequired"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /sources:
    post:
      operationId: registerSource
      tags: [Sources]
      summary: "Register a FOLDER, GIT, MAIL, or POSTGRESQL_QUERY source (DRAFT lineage)"
      description: >-
        Content-based idempotency: repeating the same body returns the same derived IDs with
        created=false. Idempotency-Key and If-Match are prohibited. Organization owner only.
      x-implementation-status: HANDLER_READY_NOT_COMPOSED
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SourceRegisterInput"
      responses:
        "200":
          $ref: "#/components/responses/SourceRegisterResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CreateForbiddenOrCSRFRejected"
        "409":
          $ref: "#/components/responses/SourceConflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /sources/connections:
    post:
      operationId: bootstrapPostgreSQLConnection
      tags: [Sources]
      summary: "Configure a DRAFT PostgreSQL connection before selecting a view"
      description: >-
        Creates only an immutable connection revision and sealed trust configuration. Does not
        create a scope, activation, workspace binding, or content job. The body accepts no DSN,
        password, SQL, tables, or columns. Idempotency-Key and If-Match are prohibited; an exact
        retry converges on the server-derived connection lineage.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [name, database_identity, lineage_id]
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 256
                database_identity:
                  type: string
                  minLength: 1
                  maxLength: 256
                lineage_id:
                  type: string
                  minLength: 1
                  maxLength: 256
                credential_reference:
                  type: string
                  pattern: '^cred_[0-7][0-9A-HJKMNP-TV-Z]{25}$'
      responses:
        "200":
          description: Immutable DRAFT connection revision
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [connection_id, connection_revision, credential_reference, created]
                properties:
                  connection_id:
                    type: string
                  connection_revision:
                    type: integer
                    format: int64
                    minimum: 1
                  credential_reference:
                    type: string
                  created:
                    type: boolean
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CreateForbiddenOrCSRFRejected"
        "409":
          $ref: "#/components/responses/SourceConflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /sources/connections/{connection_id}:discover:
    parameters:
      - name: connection_id
        in: path
        required: true
        schema:
          type: string
          pattern: '^conn_[0-7][0-9A-HJKMNP-TV-Z]{25}$'
    post:
      operationId: discoverPostgreSQLConnection
      tags: [Sources]
      summary: "Discover available PostgreSQL views before registering a scope"
      description: >-
        Enqueues a bounded catalog-only probe of the current DRAFT revision in the durable queue.
        The server determines the connection revision, trust hash, and limits. The body is empty;
        Idempotency-Key is required.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "202":
          description: Durable discovery request accepted
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [request_id, created]
                properties:
                  request_id:
                    type: string
                    pattern: '^sdrq_[0-7][0-9A-HJKMNP-TV-Z]{25}$'
                  created:
                    type: boolean
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CreateForbiddenOrCSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/IdempotencyConflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /sources/discovery/{request_id}:
    parameters:
      - name: request_id
        in: path
        required: true
        schema:
          type: string
          pattern: '^sdrq_[0-7][0-9A-HJKMNP-TV-Z]{25}$'
    get:
      operationId: getSourceDiscovery
      tags: [Sources]
      summary: "Get discovery status and the safe view catalog"
      description: >-
        Returns only the current OWNER's catalog from a still-valid encrypted result. OID, trust
        hash, credentials, and database identity are excluded; view_id is a server-owned opaque
        selector.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      responses:
        "200":
          description: Discovery state and optional terminal view inventory
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SourceDiscoveryResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /sources/discovery/{request_id}/views/{view_id}:register:
    parameters:
      - name: request_id
        in: path
        required: true
        schema:
          type: string
          pattern: '^sdrq_[0-7][0-9A-HJKMNP-TV-Z]{25}$'
      - name: view_id
        in: path
        required: true
        schema:
          type: string
          pattern: '^sdv_[0-9a-f]{64}$'
    post:
      operationId: registerDiscoveredPostgreSQLView
      tags: [Sources]
      summary: "Register the selected prepared view as a DRAFT scope"
      description: >-
        The body is empty. The server resolves the opaque view_id within the valid encrypted
        discovery result, rechecks OWNER, security epoch, trust, and connection revision, and
        creates the projection without browser-supplied schema, columns, roles, SQL, or hashes. An
        exact retry converges on request_id and view_id; mutation headers are prohibited.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
      responses:
        "200":
          $ref: "#/components/responses/SourceRegisterResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CreateForbiddenOrCSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/SourceConflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /sources/{source_scope_id}:activate:
    parameters:
      - $ref: "#/components/parameters/SourceScopeID"
    post:
      operationId: activateSourceScope
      tags: [Sources]
      summary: "Request activation of a registered source"
      description: >-
        Idempotency-Key is required and becomes the durable queue key. The body is empty. Absence
        and access denial return the same 404.
      x-implementation-status: HANDLER_READY_NOT_COMPOSED
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          $ref: "#/components/responses/SourceActivateResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/SourceConflict"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/sources:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
    get:
      operationId: listWorkspaceSources
      tags: [Sources]
      summary: "Workspace source synchronization status"
      description: "Only the current workspace revision, with current membership required."
      x-implementation-status: HANDLER_READY_NOT_COMPOSED
      security:
        - SessionCookie: []
      responses:
        "200":
          $ref: "#/components/responses/SourceListResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
    post:
      operationId: bindWorkspaceSource
      tags: [Sources]
      summary: "Bind a registered source to a workspace"
      description: >-
        Creates or re-enables the exact workspace/source projection. Source activation uses a
        separate request; this API accepts no SQL or credentials.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WorkspaceSourceBindInput"
      responses:
        "200":
          $ref: "#/components/responses/WorkspaceResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/IdempotencyConflict"
        "412":
          $ref: "#/components/responses/PreconditionRequired"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/sources/{source_scope_id}:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
      - $ref: "#/components/parameters/SourceScopeID"
    delete:
      operationId: disableWorkspaceSource
      tags: [Sources]
      summary: "Disable a source binding without deleting its lineage"
      description: >-
        Irreversible deletion is prohibited: the command creates the next immutable workspace
        revision and disables the exact binding. Retries use Idempotency-Key, and the complete tuple
        prevents stale revocation.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
        - $ref: "#/components/parameters/IfMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WorkspaceSourceRemoveInput"
      responses:
        "200":
          $ref: "#/components/responses/WorkspaceResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/IdempotencyConflict"
        "412":
          $ref: "#/components/responses/PreconditionRequired"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/questions:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
    post:
      operationId: createQuestionRun
      tags: [Search]
      summary: "Execute one Question Run in the selected answer mode"
      description: >-
        Synchronously executes a persisted Question Run with currently authorized data and
        mode-specific evidence checks. EXTRACTIVE selects source byte spans. TOOL_LOOP uses the
        shared workspace knowledge tools and records address-bound citations and tool calls;
        model_profile_id selects an available profile from this workspace's model_profiles catalog.
        The composed server defaults to TOOL_LOOP when its tool runtime is enabled; an unavailable
        generation profile fails with QUESTION_MODE_UNSUPPORTED instead of silently switching to
        EXTRACTIVE. GENERATIVE is a compatibility mode (GEN-2, ADR-0088) requiring an explicitly
        mounted workspace generation adapter; its verifier does not independently prove a claim, and
        published grounding still requires retained SOURCE_QUOTE spans. If conversation_id is
        omitted, the server creates a conversation and first turn. A supplied conversation must
        remain readable, retained, unarchived and bound to the same workspace; the new run records
        the current workspace revision, so an unrelated revision change alone does not prohibit
        continuation. This endpoint accepts no SQL text or client-defined source filters.
        Idempotency-Key is required; If-Match is prohibited.
      x-implementation-status: LIVE_EXTRACTIVE
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QuestionCreateInput"
      responses:
        "200":
          $ref: "#/components/responses/QuestionRunResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CreateForbiddenOrCSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/QuestionIdempotencyConflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/questions/{question_run_id}:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
      - $ref: "#/components/parameters/QuestionRunID"
    get:
      operationId: getQuestionRun
      tags: [Search]
      summary: "Read a Question Run with a current access check"
      description: >-
        A historical result is disclosed only if the current member can still read the workspace and
        each of its Evidence fragments.
      x-implementation-status: LIVE_EXTRACTIVE
      security:
        - SessionCookie: []
        - BearerSession: []
      responses:
        "200":
          $ref: "#/components/responses/QuestionRunResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/conversations:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
    get:
      operationId: listConversations
      tags: [Search]
      summary: "Get accessible workspace conversations"
      description: >-
        Returns only conversations visible to the current member and their opaque turn links. Every
        accessible Question Run is disclosed through the same authorization/evidence authority;
        revoked or purged data is not restored from history. Without query parameters, the previous
        first List page is returned. Optional limit (1..50, default 50) and cursor enable
        server-side keyset pagination by (created_at DESC, id DESC). A previously issued nonempty
        cursor whose workspace is no longer accessible because membership was revoked returns the
        same content-free NOT_FOUND as other access denials, rather than REQUEST_INVALID.
      x-implementation-status: LIVE_METADATA
      parameters:
        - name: limit
          in: query
          required: false
          description: >-
            Optional page size: an integer from 1..50, default 50. Duplicates, empty values, signs,
            overflow, and unknown parameters are strictly rejected.
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 50
        - name: cursor
          in: query
          required: false
          description: >-
            One nonempty opaque conversation ID identifying the last returned thread. Resolved only
            within the authorized workspace. An unknown cursor or a cursor from another workspace,
            while the requested workspace remains visible, returns the same content-free
            REQUEST_INVALID. If workspace access itself has been revoked, a previously issued cursor
            returns content-free NOT_FOUND, as with other conversation access denials, rather than
            REQUEST_INVALID. A cursor without limit is allowed.
          schema:
            $ref: "#/components/schemas/OpaqueID"
      security:
        - SessionCookie: []
        - BearerSession: []
      responses:
        "200":
          $ref: "#/components/responses/ConversationListResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/conversations/{conversation_id}:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
      - $ref: "#/components/parameters/ConversationID"
    get:
      operationId: getConversation
      tags: [Search]
      summary: "Read a conversation and its associated Question Runs"
      description: >-
        Returns server-owned metadata and append-only turns with current workspace, membership, and
        retention checks. Absence and denial are intentionally indistinguishable.
      x-implementation-status: LIVE_METADATA
      security:
        - SessionCookie: []
        - BearerSession: []
      responses:
        "200":
          $ref: "#/components/responses/ConversationResponse"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/conversations/{conversation_id}:archive:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
      - $ref: "#/components/parameters/ConversationID"
    post:
      operationId: archiveConversation
      tags: [Search]
      summary: "Archive a conversation"
      description: >-
        Irreversible server-owned archived_at transition in one transaction with an audit event and
        actor-scoped Idempotency-Key. Question and answer text is not stored in the conversation and
        cannot be recovered from it.
      x-implementation-status: LIVE_METADATA
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          $ref: "#/components/responses/ConversationResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/ConversationIdempotencyConflict"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /mcp:
    post:
      operationId: mcpJsonRpc
      tags: [Search]
      summary: Read-only MCP JSON-RPC adapter
      description: >-
        Authenticated JSON-RPC adapter for initialize, tools/list,
        knowvault_question and the conversation list/get/archive tools. It
        delegates to the same Question Run and conversation authority as the
        UI/API and never accepts SQL or arbitrary write actions.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: JSON-RPC response envelope.
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"

  # ARC-007: every live route of internal/platform/workspaceapi's dispatcher is
  # described here, and internal/platform/workspaceapi/openapi_drift_test.go
  # fails the build when a route exists without an entry. The block below closes
  # the routes that were live but undescribed (evidence, audit journal, refresh,
  # trust verification, the ADR-0087 §1 confirmation authority, the V1-C agent
  # access codes, the EMB-1 search profile and the ADR-0089 governed query).
  /sources/{source_scope_id}:sync:
    parameters:
      - $ref: "#/components/parameters/SourceScopeID"
    post:
      operationId: syncSourceScope
      tags: [Sources]
      summary: "Request a full refresh of an active source (ADR-0087 section 3)"
      description: >-
        Idempotency-Key is required. A second request while a job is live returns 409; absence and
        access denial return the same 404.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          $ref: "#/components/responses/SourceActivateResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/SourceConflict"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /sources/{source_scope_id}/documents:
    parameters:
      - $ref: "#/components/parameters/SourceScopeID"
    post:
      operationId: uploadSourceDocuments
      tags: [Sources]
      summary: "Upload documents from the browser as the source owner (UPL-1)"
      description: >-
        The source owner (source_connection.created_by) uploads one or more files (pdf, docx, pptx,
        xlsx, txt, md, html, csv) directly from the browser without root_alias/root_identity or a
        column contract. File type is checked against content, not the extension; the filename is
        sanitized. Re-uploading the same sanitized name creates a new version of the same object,
        not a new source. The action is audited and available only to the source owner, not every
        workspace member.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              additionalProperties: false
              required: [files]
              properties:
                files:
                  type: array
                  items:
                    type: string
                    format: binary
      responses:
        "200":
          description: "Accepted files with their sanitized key, format, and version."
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [connection_id, uploaded]
                properties:
                  connection_id:
                    $ref: "#/components/schemas/OpaqueID"
                  uploaded:
                    type: array
                    items:
                      type: object
                      additionalProperties: false
                      required: [name, object_key, version, format, byte_size]
                      properties:
                        name:
                          type: string
                        object_key:
                          type: string
                        version:
                          type: integer
                        format:
                          type: string
                        byte_size:
                          type: integer
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /sources/connections/{connection_id}:verify-trust:
    parameters:
      - name: connection_id
        in: path
        required: true
        schema:
          $ref: "#/components/schemas/OpaqueID"
    post:
      operationId: verifySourceConnectionTrust
      tags: [Sources]
      summary: "Verify connection trust as CONNECTOR_ADMIN (ADR-0087 section 2)"
      description: >-
        DRAFT -> VERIFIED requires attestation and separation of duties: the verifier cannot be the
        confirmer. The transition is monotonic and fails closed.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [attested_connector_identity, attested_by, attested_at]
              properties:
                attested_connector_identity:
                  type: string
                attested_by:
                  type: string
                attested_at:
                  type: string
      responses:
        "200":
          description: "Content-free trust verification result."
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/IdempotencyConflict"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/evidence/{fragment_id}:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
      - name: fragment_id
        in: path
        required: true
        schema:
          $ref: "#/components/schemas/OpaqueID"
      - name: address
        in: query
        required: false
        description: >-
          The complete canonical kv1 address of this exact fragment. When provided, reads the
          specified retained version and extraction with current workspace authorization and
          artifact integrity checks. Without address, only the current version and active extraction
          are available. An empty or repeated parameter returns REQUEST_INVALID; an invalid address,
          version/hash mismatch, purged artifact, or access denial returns the same content-free
          NOT_FOUND. Failure to read the exact version never switches the request to the current
          version.
        schema:
          type: string
          minLength: 1
      - name: scope
        in: query
        required: false
        description: >-
          FIX-7 #1: scopes the structured-source rowset returned alongside a fragment's text (no
          effect on an ordinary document fragment). Omitted or "matched" (the default) returns only
          the rows that the fragment's own citing answer actually matched: an answer of "3 vehicles"
          gets exactly 3 rows here, never the whole source. "full" explicitly requests that source
          scope's entire current snapshot; it is never the default. Any other value is
          REQUEST_INVALID (FIX-7 #2: error.fields=["scope"]).
        schema:
          type: string
          enum: [matched, full]
    get:
      operationId: getEvidenceFragment
      tags: [Search]
      summary: "Open one evidence fragment by citation identifier"
      description: >-
        Every display is reauthorized and recorded as citation.opened. Absence and access denial
        return the same 404. The addressable UI page uses #evidence/{workspace_id}/{fragment_id} and
        reads that exact workspace. The URL itself does not grant access.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      responses:
        "200":
          description: >-
            Byte-exact fragment and its anchor. Optional source_path contains the source
            path/identity decrypted by the same authorized viewer, never taken from the search
            index. address contains the structured address; canonical_address contains the complete
            fragment's kv1 address when it can be formed. is_current_version compares the fragment
            version with the object's current version during the same authorized read; false does
            not imply that an accessible newer version exists. When a public origin is configured,
            source_page_url opens a KnowVault page for the exact version address; the URL grants no
            access and contains no original file. A structured-source fragment also returns rowset
            alongside its text (FIX-2 #3 / FIX-7 #1): columns, rows (up to 50), total (the exact row
            count in the current scope before the 50-row slice), filter_label (a human-readable
            selection label such as "Selection: ...", "overdue", or "unfiltered: entire current
            source snapshot" when scope=full or no question run is found for the fragment), and
            snapshot. Ordinary document fragments have no rowset.
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/tools/list-objects:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
      - name: offset
        in: query
        required: false
        description: >-
          R3a-1 KV-A02: zero-based object-inventory page offset. Only an unsigned decimal int64
          representation is accepted; signs, empty values, duplicates, unknown keys, and overflow
          return REQUEST_INVALID.
        schema:
          type: string
          pattern: "^[0-9]{1,19}$"
      - name: limit
        in: query
        required: false
        description: >-
          Page size in objects. 0, like omission, selects the default of 100 objects. Requested
          values are capped at 1000; the effective value is always returned as limit.
        schema:
          type: string
          pattern: "^[0-9]{1,19}$"
      - name: all_versions
        in: query
        required: false
        description: >-
          false (default) includes only each object's current version; true includes every version
          of every workspace object.
        schema:
          type: string
          enum: ["true", "false"]
    get:
      operationId: listWorkspaceToolObjects
      tags: [Search]
      summary: "Paginated workspace object inventory (REST parity with knowvault_list_objects)"
      description: >-
        R3a-1 KV-A02: REST parity with the knowvault_list_objects MCP tool. Returns the same
        projection as MCP: one row per (source_object_id, source_version_id), with version,
        external_version_key, content_hash, observed_at, object_type, and an immutable address, plus
        an explicit offset/limit/has_more/next_offset page window. A limit never silently truncates
        the result. Dispatches through the same authorized Evidence viewer, preserving
        admission-before-data, audit, and content-free denial. An unknown workspace or a nonmember
        receives the same 404 NOT_FOUND, without rows or an echo of the requested workspace.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      responses:
        "200":
          description: >-
            Explicit object inventory page: objects, offset, effective limit, has_more, and
            next_offset when another page exists.
          content:
            application/json:
              schema:
                type: object
                properties:
                  objects:
                    type: array
                    items:
                      type: object
                  offset:
                    type: integer
                    format: int64
                  limit:
                    type: integer
                    format: int64
                  has_more:
                    type: boolean
                  next_offset:
                    type: integer
                    format: int64
                    nullable: true
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
    post:
      operationId: listWorkspaceToolObjectsPost
      tags: [Search]
      summary: "Paginated workspace object inventory (POST parity)"
      description: >-
        The same REST parity with knowvault_list_objects as GET: offset/limit/all_versions are query
        parameters, and the request body is not read. POST supports clients that call knowledge
        tools using POST; projection, authorization, audit, and denial are identical to GET.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      responses:
        "200":
          description: "The same explicit object inventory page as GET."
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/tools/search:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
      - name: query
        in: query
        required: true
        description: >-
          R3a-1 KV-A03: lexical query over canonical workspace fragments. Required and nonempty;
          whitespace-only strings are rejected. Limited to 4096 characters. A duplicate, unknown
          key, or empty value returns REQUEST_INVALID before any read.
        schema:
          type: string
          minLength: 1
          maxLength: 4096
      - name: offset
        in: query
        required: false
        description: >-
          Zero-based result-page offset. Only an unsigned decimal int64 representation is accepted;
          signs, empty values, duplicates, unknown keys, and overflow return REQUEST_INVALID.
        schema:
          type: string
          pattern: "^[0-9]{1,19}$"
      - name: limit
        in: query
        required: false
        description: >-
          Page size in hits. 0, like omission, selects the default of 20. Requested values are
          capped at 100; the effective value is always returned as limit.
        schema:
          type: string
          pattern: "^[0-9]{1,19}$"
      - name: all_versions
        in: query
        required: false
        description: >-
          false (default) includes only each object's current version; true includes every version
          of every workspace object.
        schema:
          type: string
          enum: ["true", "false"]
    get:
      operationId: searchWorkspaceToolDocuments
      tags: [Search]
      summary: "Keyword and semantic search over workspace documents (REST parity with knowvault_search)"
      description: >-
        R3a-1 KV-A03 + S3: REST parity with the knowvault_search MCP tool. Returns the same
        projection as MCP: hits with excerpt, score, channel, version_id, observed_at, content_hash,
        and an immutable address, plus an explicit offset/limit/has_more/next_offset page window. A
        limit never silently truncates the result.

        S3: search is hybrid. The lexical and vector channels each apply workspace,
        permission-bearing source-scope, and current-version-state filtering within the query before
        ranking. RRF fusion follows, and every surviving hit is then reauthorized against live
        state. A permission revoked after indexing therefore removes the hit regardless of which
        channel found it. A deployment without a vector channel returns the same lexical page and
        declares profile.degraded=true rather than presenting lexical retrieval as hybrid.

        The per-call search profile is not a tool argument: it arrives in the
        X-KnowVault-Search-Profile header, is accepted only from the workspace owner, is recorded in
        audit, and is returned as profile. The model does not choose the algorithm, which would
        otherwise make algorithm selection part of the answer.

        Dispatches through the same authorized Evidence viewer, preserving admission-before-data,
        audit, and content-free denial. An unknown workspace or a nonmember receives the same 404
        NOT_FOUND, without hits or an echo of the requested workspace.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      responses:
        "200":
          description: >-
            Explicit result page: results, offset, effective limit, has_more, and next_offset when
            another page exists. Also includes profile, the search profile actually used for this
            call, and terms alongside ordinary results when the workspace defines the queried term.
          content:
            application/json:
              schema:
                type: object
                properties:
                  results:
                    type: array
                    items:
                      type: object
                      properties:
                        channel:
                          type: string
                          description: >-
                            S3: the origin of a hit: lexical (matching words), vector (matching
                            meaning), hybrid (both), or term (a workspace term definition). This is
                            result provenance, not a parameter: channel is never accepted as input.
                          enum: [lexical, vector, hybrid, term]
                        version_state:
                          type: string
                          description: >-
                            Version state under which the hit was returned. Omitted when the result
                            path cannot establish it; never guessed.
                          enum: [CURRENT, SUPERSEDED]
                        score:
                          type: number
                          description: >-
                            Ranking score. Hybrid retrieval uses a fractional RRF score; a purely
                            lexical deployment uses an integer term frequency. Both are numbers.
                  profile:
                    type: object
                    description: >-
                      S3 section 7.5: the search profile actually used for the call (mode, lexical,
                      vector, fusion, k, reranker, degraded, embedding_profile). It is not a tool
                      argument; the model does not choose the algorithm. degraded=true means a
                      declared channel did not respond, so a nominally hybrid result that was
                      actually lexical is not presented as hybrid.
                  terms:
                    type: array
                    description: >-
                      S3 section 4.1: workspace term definitions matching the query text, returned
                      as separately addressable hits alongside ordinary results, never instead of
                      them. A homonym produces two hits; the product does not choose between them.
                      This member is omitted when the workspace catalog defines nothing.
                    items:
                      type: object
                  terms_truncated:
                    type: boolean
                  partial:
                    type: boolean
                    description: >-
                      Ranking candidates are not fully covered, or the page size is constrained by
                      the evidence-read budget. has_more and next_offset indicate continuation
                      within the discovered candidate set; partial=true does not imply that no other
                      workspace data exists.
                  offset:
                    type: integer
                    format: int64
                  limit:
                    type: integer
                    format: int64
                  has_more:
                    type: boolean
                  next_offset:
                    type: integer
                    format: int64
                    nullable: true
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
    post:
      operationId: searchWorkspaceToolDocumentsPost
      tags: [Search]
      summary: "Lexical search over workspace documents (POST parity)"
      description: >-
        The same REST parity with knowvault_search as GET: query/offset/limit/all_versions are query
        parameters, and the request body is not read. POST supports clients that call knowledge
        tools using POST; projection, authorization, audit, and denial are identical to GET.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      responses:
        "200":
          description: "The same explicit result page as GET."
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/tools/grep:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
      - name: pattern
        in: query
        required: true
        description: >-
          R3a-1 KV-A02c: Go/RE2 regular expression over canonical workspace object text. Required
          and nonempty; whitespace-only strings are rejected. Limited to 4096 characters. An invalid
          expression returns REQUEST_INVALID before any read.
        schema:
          type: string
          minLength: 1
          maxLength: 4096
      - name: offset
        in: query
        required: false
        description: >-
          Zero-based match-page offset. Only an unsigned decimal int64 representation is accepted;
          signs, empty values, duplicates, unknown keys, and overflow return REQUEST_INVALID.
        schema:
          type: string
          pattern: "^[0-9]{1,19}$"
      - name: limit
        in: query
        required: false
        description: >-
          Page size in matches. 0, like omission, selects the default of 20. Requested values are
          capped at 100; the effective value is always returned as limit.
        schema:
          type: string
          pattern: "^[0-9]{1,19}$"
      - name: all_versions
        in: query
        required: false
        description: >-
          false (default) includes only each object's current version; true includes every version
          of every workspace object.
        schema:
          type: string
          enum: ["true", "false"]
      - name: ref
        in: query
        required: false
        description: >-
          Optional immutable ref of a registered Git source. When provided, only GIT_FILE versions
          whose external version identity exactly matches ref are scanned. It is neither a file path
          nor a document selector.
        schema:
          type: string
          minLength: 1
          maxLength: 256
      - name: address
        in: query
        required: false
        description: >-
          Complete canonical kv1 address of one text object/version returned by a knowledge tool.
          The regex runs only against this authorized object without scanning the inventory. address
          is mutually exclusive with ref and all_versions; malformed or inaccessible addresses are
          rejected.
        schema:
          type: string
          minLength: 1
    get:
      operationId: grepWorkspaceToolDocuments
      tags: [Search]
      summary: "Exact regex grep over workspace objects (REST parity with knowvault_grep)"
      description: >-
        R3a-1 KV-A02c: REST parity with the knowvault_grep MCP tool. Returns the same projection as
        MCP: matches with offset/length within canonical text, excerpt, match_hash, version_id,
        observed_at, content_hash, and an immutable address (including canonical_address), plus an
        explicit offset/limit/has_more/next_offset page window. A limit never silently truncates the
        result. Dispatches through the same authorized EvidenceGrep capability as MCP, preserving
        admission-before-data, audit, and content-free denial. An unknown workspace or a nonmember
        receives the same 404 NOT_FOUND, without matches or an echo of the requested workspace. With
        address, the existing authorized whole-object read retrieves one exact object/version
        without scanning the inventory. Each match also contains read_fragments: up to eight
        addresses of complete overlapping fragments in source order, read_fragments_total, and
        read_fragments_has_more. Addresses are tied to the source version and allow complete
        fragment reading through the ordinary read tool; long fragments are paginated using
        has_more/next_offset.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      responses:
        "200":
          description: "Explicit match page: matches, offset, effective limit, has_more, and next_offset when another page exists."
          content:
            application/json:
              schema:
                type: object
                properties:
                  matches:
                    type: array
                    items:
                      type: object
                      properties:
                        read_fragments:
                          type: array
                          maxItems: 8
                          description: "Complete fragments overlapping the match, in source order."
                          items:
                            type: object
                            additionalProperties: false
                            required: [canonical_address, fragment_id, length]
                            properties:
                              canonical_address:
                                type: string
                                description: "Complete fragment address with its source version and organization-keyed span hash."
                              fragment_id:
                                type: string
                              length:
                                type: integer
                                format: int64
                                minimum: 1
                                description: "Complete fragment length in UTF-8 bytes."
                        read_fragments_total:
                          type: integer
                          format: int64
                          minimum: 1
                          description: "Count of all fragments overlapping the match."
                        read_fragments_has_more:
                          type: boolean
                          description: >-
                            True when more than eight fragments overlap; the complete range remains
                            accessible through the match's canonical_address.
                  offset:
                    type: integer
                    format: int64
                  limit:
                    type: integer
                    format: int64
                  has_more:
                    type: boolean
                  next_offset:
                    type: integer
                    format: int64
                    nullable: true
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: "Address span verification did not match the text read."
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
    post:
      operationId: grepWorkspaceToolDocumentsPost
      tags: [Search]
      summary: "Exact regex grep over workspace objects (POST parity)"
      description: >-
        The same REST parity with knowvault_grep as GET:
        pattern/offset/limit/all_versions/ref/address are query parameters; the request body is not
        a parameter channel, and a nonempty body returns REQUEST_INVALID. POST supports clients that
        call knowledge tools using POST; projection, authorization, audit, and denial are identical
        to GET.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      responses:
        "200":
          description: "The same explicit match page as GET."
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: "Address span verification did not match the text read."
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/tools/related:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
      - name: fragment_id
        in: query
        required: false
        description: >-
          R3a-1: identifier of the addressed object whose canonical cross-source relations are
          requested. Required unless address is supplied; limited to 128 characters. Duplicates,
          empty values, and unknown keys return REQUEST_INVALID before any read.
        schema:
          type: string
          minLength: 1
          maxLength: 128
      - name: address
        in: query
        required: false
        description: >-
          Canonical compact internal/address value (kv1:<object>:<version>:<span>:<hash16>). Parsed
          with address.Parse. When fragment_id is absent, it selects the object; when both are
          present, they must identify the same object. An unparseable address or an object mismatch
          with fragment_id returns REQUEST_INVALID before invoking the capability.
        schema:
          type: string
          minLength: 1
      - name: direction
        in: query
        required: false
        description: >-
          Closed direction vocabulary: referencing, references, or both (default). Any other value
          returns REQUEST_INVALID.
        schema:
          type: string
          enum: ["referencing", "references", "both"]
      - name: cursor
        in: query
        required: false
        description: >-
          Stable result-set cursor in v1:<offset> form. Omit it for the first page; pass the
          preceding page's next_cursor to continue. A cursor not issued by this server returns
          REQUEST_INVALID.
        schema:
          type: string
          pattern: "^v1:[0-9]{1,19}$"
      - name: limit
        in: query
        required: false
        description: >-
          Page size in relations. 0, like omission, selects the default of 50. Requested values are
          capped at 200; the effective value is always returned as limit.
        schema:
          type: string
          pattern: "^[0-9]{1,19}$"
    get:
      operationId: relatedWorkspaceToolObjects
      tags: [Search]
      summary: "Canonical cross-source relations of a workspace object (REST parity with knowvault_related)"
      description: >-
        R3a-1: REST parity with the knowvault_related MCP tool. Returns the same projection as MCP:
        relations with relation_kind, a bounded excerpt, fragment_id, version_id, observed_at,
        content_hash, and an immutable address, plus direction, effective limit, has_more,
        next_cursor, and truncated. A limit never silently truncates the result. Dispatches through
        the same authorized EvidenceRelated capability as MCP, preserving admission-before-data,
        audit, and content-free denial. An unknown workspace or a nonmember receives the same 404
        NOT_FOUND, without relations or an echo of the requested workspace.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      responses:
        "200":
          description: >-
            Explicit relation page: relations, direction, effective limit, has_more, next_cursor
            when another page exists, and truncated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  relations:
                    type: array
                    items:
                      type: object
                  direction:
                    type: string
                  limit:
                    type: integer
                    format: int64
                  has_more:
                    type: boolean
                  next_cursor:
                    type: string
                    nullable: true
                  truncated:
                    type: boolean
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
    post:
      operationId: relatedWorkspaceToolObjectsPost
      tags: [Search]
      summary: "Canonical cross-source relations of a workspace object (POST parity)"
      description: >-
        The same REST parity with knowvault_related as GET:
        fragment_id/address/direction/cursor/limit are query parameters; the request body is not a
        parameter channel, and a nonempty body returns REQUEST_INVALID. POST supports clients that
        call knowledge tools using POST; projection, authorization, audit, and denial are identical
        to GET.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      responses:
        "200":
          description: "The same explicit relation page as GET."
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/tools/sources:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
    get:
      operationId: listWorkspaceToolSources
      tags: [Sources]
      summary: "Workspace source and schedule inventory (REST parity with knowvault_sources_list)"
      description: >-
        R3a-1 Outcome 2: REST parity with the knowvault_sources_list MCP tool. Returns the same
        projection as MCP and the existing GET /workspaces/{id}/sources route: one entry per bound
        source with complete synchronization status, schedule fields, trust status accounting for
        separation of duties, a confirmation-state label, and a shared confirmation_context.
        Composed from the same injected SourceService.ListSources and
        SourceService.ConfirmationContext reads as the MCP tool, preserving authorization,
        content-free denial, and audit. No query parameters are accepted: an unknown key, duplicate,
        empty value, or bare '?' returns REQUEST_INVALID before any read. An unknown workspace or a
        nonmember receives the same 404 NOT_FOUND without source content or an echo of the requested
        workspace; an unauthenticated call returns 401. A service without the source-read capability
        fails closed with 503 SERVICE_UNAVAILABLE, the REST equivalent of MCP -32000.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      responses:
        "200":
          description: >-
            Workspace source inventory and confirmation_context: the same projection as the
            knowvault_sources_list MCP tool's structuredContent.
          content:
            application/json:
              schema:
                type: object
                properties:
                  sources:
                    type: array
                    items:
                      type: object
                  confirmation_context:
                    type: object
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
    post:
      operationId: listWorkspaceToolSourcesPost
      tags: [Sources]
      summary: "Workspace source and schedule inventory (POST parity)"
      description: >-
        The same REST parity with knowvault_sources_list as GET: no parameters are supplied, and the
        request body is not a parameter channel; a nonempty body returns REQUEST_INVALID. POST
        supports clients that call knowledge tools using POST. Projection, authorization, audit, and
        denial are identical to GET, and the CSRF gate for other unsafe methods applies.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      responses:
        "200":
          description: "The same {sources, confirmation_context} projection as GET."
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
  /workspaces/{workspace_id}/tools/refresh:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
      - name: source_scope_id
        in: query
        required: false
        description: >-
          R3a-1: optional source selector. Restricts refresh to exactly one source scope bound to
          the workspace. A nonempty string of at most 128 characters; duplicates, empty values, and
          unknown keys return REQUEST_INVALID before invoking the service.
        schema:
          type: string
          minLength: 1
          maxLength: 128
      - name: offset
        in: query
        required: false
        description: "Workspace source-list page offset, default 0. A nondecimal value or a sign returns REQUEST_INVALID."
        schema:
          type: string
          pattern: "^[0-9]{1,19}$"
      - name: limit
        in: query
        required: false
        description: >-
          Source-list page size. 0, like omission, means 100. Requested values are capped at 1000;
          the effective value is always returned as limit.
        schema:
          type: string
          pattern: "^[0-9]{1,19}$"
    get:
      operationId: refreshWorkspaceToolSources
      tags: [Sources]
      summary: "Refresh workspace sources where permitted (REST parity with knowvault_refresh)"
      description: >-
        R3a-1 Outcome 2: REST parity with the knowvault_refresh MCP tool. Reads workspace-bound
        sources through the same injected SourceService.ListSources as GET /workspaces/{id}/sources,
        and enqueues refresh jobs for permitted sources through the same authorized
        SourceService.Sync as POST /sources/{scope}:sync. Returns the same projection as MCP
        structuredContent: refreshed[{source_scope_id,job_id}], skipped[{source_scope_id,reason}],
        and an explicit offset/limit/has_more/next_offset window. An unknown workspace or a
        nonmember receives content-free 404 NOT_FOUND without source content or an echo of the
        workspace; an unauthenticated call returns 401. A service without source capabilities fails
        closed with 503 SERVICE_UNAVAILABLE, the REST equivalent of MCP -32000. The request body is
        not a parameter channel; a nonempty body returns REQUEST_INVALID.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      responses:
        "200":
          description: "Source-refresh page: the same projection as the knowvault_refresh MCP tool's structuredContent."
          content:
            application/json:
              schema:
                type: object
                properties:
                  refreshed:
                    type: array
                    items:
                      type: object
                  skipped:
                    type: array
                    items:
                      type: object
                  offset:
                    type: integer
                  limit:
                    type: integer
                  has_more:
                    type: boolean
                  next_offset:
                    type: integer
                    nullable: true
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: "Refresh conflicts with an existing refresh job for this source."
          content:
            application/json:
              schema:
                type: object
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
    post:
      operationId: refreshWorkspaceToolSourcesPost
      tags: [Sources]
      summary: "Refresh workspace sources (POST parity)"
      description: >-
        The same REST parity with knowvault_refresh as GET: source_scope_id, offset, and limit are
        query parameters; the request body is not a parameter channel, and a nonempty body returns
        REQUEST_INVALID. POST supports clients that call knowledge tools using POST. Projection,
        authorization, audit, and denial are identical to GET, and the CSRF gate for other unsafe
        methods applies.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      responses:
        "200":
          description: "The same refreshed/skipped/paging projection as GET."
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: "Refresh conflicts with an existing refresh job for this source."
          content:
            application/json:
              schema:
                type: object
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
  /workspaces/{workspace_id}/tools/read:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
      - name: fragment_id
        in: query
        required: false
        description: >-
          R3a-1: identifier of the addressed fragment (citation object) read in explicit pages.
          Required unless address is supplied; limited to 128 characters. Duplicates, empty values,
          and unknown keys return REQUEST_INVALID before any read.
        schema:
          type: string
          minLength: 1
          maxLength: 128
      - name: address
        in: query
        required: false
        description: >-
          Canonical compact internal/address value (kv1:<object>:<version>:<span>:<hash16>). Parsed
          with address.Parse. When fragment_id is absent, it selects the object; when both are
          present, they must identify the same object. An unparseable address, object mismatch with
          fragment_id, or source/version mismatch returns REQUEST_INVALID before invoking the
          capability. A keyed span-digest mismatch returns 409 EVIDENCE_SPAN_HASH_MISMATCH without
          page bytes.
        schema:
          type: string
          minLength: 1
      - name: offset
        in: query
        required: false
        description: >-
          Page offset in UTF-8 bytes of the fragment's canonical text, default 0. A nondecimal
          value, sign, or offset beyond the end of the text returns REQUEST_INVALID. In cursor mode,
          a nonzero offset is incompatible with cursor.
        schema:
          type: string
          pattern: "^[0-9]{1,19}$"
      - name: limit
        in: query
        required: false
        description: >-
          Page size in bytes. 0, like omission, means 4096. Requested values are capped at 65536;
          the effective value is always returned as limit.
        schema:
          type: string
          pattern: "^[0-9]{1,19}$"
      - name: expected_span_hash
        in: query
        required: false
        description: >-
          Expected keyed text_hash of the entire fragment. A mismatch returns the typed content-free
          error 409 EVIDENCE_SPAN_HASH_MISMATCH without page text or an address, detecting a
          substituted index.
        schema:
          type: string
          minLength: 1
      - name: cursor
        in: query
        required: false
        description: >-
          Stable whole-object page cursor in v1:<offset> form. Supplying cursor switches reading to
          pages of the version's entire source text; request the first page with cursor=v1:0, then
          use the preceding page's next_cursor. Another form, an empty value, a cursor beyond the
          end of the original, or combination with a nonzero offset returns REQUEST_INVALID.
        schema:
          type: string
          pattern: "^v1:[0-9]{1,19}$"
    get:
      operationId: readWorkspaceToolEvidence
      tags: [Search]
      summary: "Explicit fragment or whole-object pages within a workspace (REST parity with knowvault_read)"
      description: >-
        R3a-1 Outcome 1: REST parity with the knowvault_read MCP tool. Returns the same projection
        as MCP. Fragment mode returns fragment_id, text, text_base64, offset, length, next_offset,
        has_more, effective limit, total_length, text_hash, page_hash, immutable address and
        canonical_address, and optional neighbors containing immediate-neighbor addresses for a
        complete direct fragment page starting at offset=0. Whole-object mode returns offset,
        length, next_cursor, has_more, complete, effective limit, total_bytes, whole_hash,
        page_hash, address, canonical_address, fragment_count, ordinal_start, and ordinal_end.
        Concatenating decoded text_base64 from all pages reproduces the retained text assembly
        hashed by whole_hash; a limit never silently truncates it. Dispatches through the same
        authorized Evidence.Read and EvidenceWholeObject/address.Read capabilities as MCP,
        preserving admission-before-data, audit, and content-free denial. An unknown workspace or a
        nonmember receives the same 404 NOT_FOUND without text or an echo of the requested
        workspace. With address, a separate exact read permits a retained superseded version and its
        immutable extraction; fragment_id without address continues to read only current data.
        Current permissions and version/extraction retention are checked on every call.
        is_current_version indicates version currency; source_page_url opens the authorized fragment
        in KnowVault at its exact address. In whole-object mode, the page link identifies the anchor
        fragment. text_representation distinguishes exact canonical text from the newer extraction
        layout (CANONICAL_TEXT_V1_LAYOUT_V2) from legacy fragment concatenation
        (LEGACY_FRAGMENT_CONCAT_V1), for which preservation of original line breaks is unproven. The
        hash always covers the bytes actually returned.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      responses:
        "200":
          description: >-
            Explicit page: text, text_base64, offset, length, next_offset or next_cursor, has_more,
            effective limit, and hashes.
          content:
            application/json:
              schema:
                type: object
                properties:
                  fragment_id:
                    type: string
                  text:
                    type: string
                  text_base64:
                    type: string
                  offset:
                    type: integer
                    format: int64
                  length:
                    type: integer
                    format: int64
                  next_offset:
                    type: integer
                    format: int64
                    nullable: true
                  next_cursor:
                    type: string
                    nullable: true
                  has_more:
                    type: boolean
                  complete:
                    type: boolean
                  limit:
                    type: integer
                    format: int64
                  total_length:
                    type: integer
                    format: int64
                  total_bytes:
                    type: integer
                    format: int64
                  text_hash:
                    type: string
                  whole_hash:
                    type: string
                  text_representation:
                    type: string
                    enum: [LEGACY_FRAGMENT_CONCAT_V1, CANONICAL_TEXT_V1_LAYOUT_V2, CANONICAL_STRUCTURAL_TEXT_V1]
                    description: Present in whole-object mode. TEXT layout-v2 preserves the canonical text stream; structural-text-v1 preserves the native parser's canonical object text and LF separators between its units. Legacy concatenation has no complete-text fidelity proof. None denotes the original binary file.
                  page_hash:
                    type: string
                  address:
                    type: object
                  canonical_address:
                    type: string
                  neighbors:
                    type: object
                    description: >-
                      Only for a complete direct fragment page (offset=0, has_more=false). Contains
                      at most the preceding and following fragments of the same active extraction,
                      without their text.
                    properties:
                      previous:
                        nullable: true
                        allOf:
                          - $ref: "#/components/schemas/EvidenceFragmentNeighbor"
                      next:
                        nullable: true
                        allOf:
                          - $ref: "#/components/schemas/EvidenceFragmentNeighbor"
                  source_path:
                    type: string
                    description: Authorized external source identity, such as a relative file path; absent or empty for legacy objects without that artifact.
                  fragment_count:
                    type: integer
                  fragments:
                    type: array
                    description: Fragment addresses intersecting this page; byte windows are relative to its text.
                    items:
                      type: object
                      required: [canonical_address, fragment_id, page_offset, length]
                      properties:
                        canonical_address:
                          type: string
                        fragment_id:
                          type: string
                        page_offset:
                          type: integer
                        length:
                          type: integer
                  ordinal_start:
                    type: integer
                  ordinal_end:
                    type: integer
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: >-
            Content-free span-hash denial: expected_span_hash or the address's keyed digest does not
            match the retained text_hash (MCP -32005). Returns neither page text nor an address.
          $ref: "#/components/responses/ErrorResponse"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
    post:
      operationId: readWorkspaceToolEvidencePost
      tags: [Search]
      summary: "Explicit fragment or whole-object pages within a workspace (POST parity)"
      description: >-
        The same REST parity with knowvault_read as GET:
        fragment_id/address/offset/limit/expected_span_hash/cursor are query parameters; the request
        body is not a parameter channel, and a nonempty body returns REQUEST_INVALID. POST supports
        clients that call knowledge tools using POST; projection, authorization, audit, and denial
        are identical to GET.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      responses:
        "200":
          description: "The same explicit page as GET."
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: "Content-free span-hash denial, as with GET (MCP -32005)."
          $ref: "#/components/responses/ErrorResponse"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/audit-events:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
    get:
      operationId: listWorkspaceAuditEvents
      tags: [Audit]
      summary: "Workspace event selection and organization chain head"
      description: >-
        Returns only a content-free projection of workspace events and the head of the entire
        organization's hash chain. Also includes read, search, and inventory denials whose closed
        record form identifies this workspace in resource_id while workspace_id is empty. Original
        audit records are unchanged; other workspaces and unrelated organization events are
        excluded. The head belongs to the organization, not the workspace, and is included only to
        connect events to authoritative append state. Workspace events form a selection, not a
        continuous workspace chain: not every organization record belongs to this workspace, so this
        list cannot establish chain continuity. Document bodies, answers, and SQL are never
        included. Without query parameters, returns the first page of 100 newest events. Optional
        before_sequence is a positive decimal int64 string; each page contains up to 100 events with
        sequence strictly below the cursor, ordered by sequence DESC. When another page exists,
        next_before_sequence is the decimal-string sequence of the last returned record. The cursor
        is not a JavaScript Number. Every page rechecks the tenant/workspace/audit.read_metadata
        gate and is recorded as audit.viewed.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      parameters:
        - name: before_sequence
          in: query
          required: false
          description: >-
            Keyset cursor: a positive decimal int64 string. Duplicates, an empty value, a sign,
            unknown parameters (including limit), overflow, a minus sign, semicolon, or malformed
            escaping return REQUEST_INVALID.
          schema:
            type: string
            pattern: "^[0-9]{1,19}$"
      responses:
        "200":
          description: "Audit events, head_hash of the organization's continuous chain, and optional next_before_sequence."
          content:
            application/json:
              schema:
                type: object
                properties:
                  workspace_id:
                    type: string
                  head_sequence:
                    type: integer
                    format: int64
                  head_hash:
                    type: string
                  truncated:
                    type: boolean
                  next_before_sequence:
                    type: string
                    description: "Present only when another page exists."
                  events:
                    type: array
                    items:
                      type: object
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/confirmation-grants:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
    post:
      operationId: issueConfirmationGrant
      tags: [Sources]
      summary: "Issue a workspace source-confirmation grant (ADR-0053, ADR-0087 section 1)"
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [expected_workspace_revision, expected_workspace_configuration_hash, target_principal_id, ttl_seconds, expected_policy_revision]
              properties:
                expected_workspace_revision:
                  type: integer
                expected_workspace_configuration_hash:
                  type: string
                target_principal_id:
                  type: string
                ttl_seconds:
                  type: integer
                expected_policy_revision:
                  type: string
      responses:
        "200":
          description: "Canonical authority-command result: identifier and hash."
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/IdempotencyConflict"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/confirmation-grants/{grant_id}:revoke:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
      - name: grant_id
        in: path
        required: true
        schema:
          $ref: "#/components/schemas/OpaqueID"
    post:
      operationId: revokeConfirmationGrant
      tags: [Sources]
      summary: "Revoke a confirmation grant (ADR-0053, ADR-0087 section 1)"
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [grant_revision, grant_hash, expected_policy_revision]
              properties:
                grant_revision:
                  type: integer
                grant_hash:
                  type: string
                expected_policy_revision:
                  type: string
      responses:
        "200":
          description: "Canonical revocation result."
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/IdempotencyConflict"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/managed-source-confirmations:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
    post:
      operationId: confirmManagedSource
      tags: [Sources]
      summary: "Confirm a workspace-managed source (ADR-0053, ADR-0087 section 1)"
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
      responses:
        "200":
          description: "Canonical confirmation result."
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/IdempotencyConflict"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/managed-source-confirmations/{confirmation_id}:revoke:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
      - name: confirmation_id
        in: path
        required: true
        schema:
          $ref: "#/components/schemas/OpaqueID"
    post:
      operationId: revokeManagedSourceConfirmation
      tags: [Sources]
      summary: "Revoke source confirmation, enabling reconfirmation (ADR-0087 section 1)"
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
      responses:
        "200":
          description: "Canonical confirmation-revocation result."
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/IdempotencyConflict"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/access-codes:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
    get:
      operationId: listAccessCodes
      tags: [Access]
      summary: "Workspace agent access codes (V1-C, ADR-0079 section 3)"
      description: >-
        Workspace OWNER only. Content-free projection: identifiers, name, workspaces, expiry, and
        status. The code itself is displayed exactly once at issuance and is not stored in
        plaintext.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
        - BearerSession: []
      responses:
        "200":
          description: "List of issued codes without their secret values."
          content:
            application/json:
              schema:
                type: object
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"
    post:
      operationId: issueAccessCode
      tags: [Access]
      summary: "Issue a service-principal access code scoped to workspaces"
      description: >-
        The code is displayed once. OWNER is required for every selected workspace; the route
        workspace is included automatically. SERVICE receives ordinary MEMBER rights and an
        allow-list of up to ten canonical knowledge tools: knowvault_question,
        knowvault_evidence_get, knowvault_search, knowvault_read, knowvault_list_objects,
        knowvault_related, knowvault_grep, knowvault_sources, knowvault_refresh and
        knowvault_governed_query_ask. Search, related and grep are advertised only when their
        evidence capabilities are composed. The governed SQL ask additionally requires workspace
        opt-in, an exposed connection and database grants; listing it does not qualify it as part of
        the prepared-snapshot pilot. Tool authorization remains in force, and administrative tools
        are unavailable. Another workspace returns the same response as a nonexistent workspace. The
        code is accepted only at the MCP endpoint. If-Match is not required. An exact issuance retry
        returns 409 SERVICE_PRINCIPAL_ALREADY_ISSUED without repeating the secret.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [name, workspace_ids, ttl_seconds]
              properties:
                name:
                  type: string
                  minLength: 1
                  description: "Nonempty name, at most 256 UTF-8 bytes, without CR, LF, or NUL."
                workspace_ids:
                  type: array
                  minItems: 1
                  maxItems: 20
                  items:
                    $ref: "#/components/schemas/OpaqueID"
                ttl_seconds:
                  type: integer
                  minimum: 3600
                  maximum: 15552000
      responses:
        "201":
          description: "One-time display of the issued code and its metadata."
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/IdempotencyConflict"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/access-codes/{credential_id}:revoke:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
      - name: credential_id
        in: path
        required: true
        schema:
          $ref: "#/components/schemas/OpaqueID"
    post:
      operationId: revokeAccessCode
      tags: [Access]
      summary: "Immediately revoke an agent access code"
      description: >-
        Requires OWNER of every workspace covered by the code. The next MCP call with the revoked
        code returns 401. Revoking an already revoked code returns 404. If-Match is not required.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          description: "The code is revoked; the response body contains revoked=true."
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/search-profile:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
    get:
      operationId: getSearchProfile
      tags: [Search]
      summary: "Current workspace search profile and transition status (EMB-1)"
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      responses:
        "200":
          description: "Active profile revision and current transition state."
          content:
            application/json:
              schema:
                type: object
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/search-profile:revise:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
    post:
      operationId: reviseSearchProfile
      tags: [Search]
      summary: "Move the workspace to the mounted embedding profile (EMB-1)"
      description: >-
        The entire corpus is reindexed under the new profile, and the switch occurs only after
        completion. Until then, answers use the profile under which the corpus is indexed. Safe to
        repeat.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          description: "Profile revision to which the workspace has moved."
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/IdempotencyConflict"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/metric-definitions:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
    get:
      operationId: listWorkspaceMetricDefinitions
      tags: [MetricDefinition]
      summary: "Workspace metric-definition versions (R2)"
      description: >-
        Read-only catalog of all issued workspace metric-definition versions. Definition access is
        rechecked like any protected data: a caller without workspace access receives the same 404
        NOT_FOUND as other protected reads, without revealing that a definition exists. There is no
        mutation here; DRAFT creation and version approval remain with the workspace owner's
        authority, which owns the audit.
      x-implementation-status: HANDLER_READY_NOT_COMPOSED
      security:
        - SessionCookie: []
      responses:
        "200":
          description: "All issued versions of every definition in the workspace."
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [definitions]
                properties:
                  definitions:
                    type: array
                    items:
                      $ref: "#/components/schemas/MetricDefinition"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/metric-definitions/{metric_id}/versions/{version}:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
      - name: metric_id
        in: path
        required: true
        schema:
          $ref: "#/components/schemas/OpaqueID"
      - name: version
        in: path
        required: true
        description: "Monotonic definition version number: a positive decimal integer without a sign or leading zeros."
        schema:
          type: integer
          minimum: 1
          maximum: 9007199254740991
    get:
      operationId: getWorkspaceMetricDefinitionVersion
      tags: [MetricDefinition]
      summary: "Exact metric-definition version (R2)"
      description: >-
        Read-only access to exactly one definition version by ID and version number. Access is
        rechecked as for the version catalog; denial and an absent version are indistinguishable and
        return 404 NOT_FOUND.
      x-implementation-status: HANDLER_READY_NOT_COMPOSED
      security:
        - SessionCookie: []
      responses:
        "200":
          description: "Requested metric-definition version."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MetricDefinition"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/metric-definitions:draft:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
    post:
      operationId: createWorkspaceMetricDefinitionDraft
      tags: [MetricDefinition]
      summary: "Draft a metric definition (R2, owner only)"
      description: >-
        Creates version 1 of a new metric definition, or a new monotonically increasing draft
        version when that definition's current version is approved. An approved version is
        immutable. Definition access is rechecked like any protected action: denial and absence are
        indistinguishable and return 404 NOT_FOUND. The body contains only product definition
        fields; client-supplied version, status, workspace, and author are never accepted.
      x-implementation-status: HANDLER_READY_NOT_COMPOSED
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MetricDefinitionDraftRequest"
      responses:
        "200":
          description: "Created draft metric-definition version."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MetricDefinition"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/metric-definitions/{metric_id}:approve:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
      - name: metric_id
        in: path
        required: true
        schema:
          $ref: "#/components/schemas/OpaqueID"
    post:
      operationId: approveWorkspaceMetricDefinition
      tags: [MetricDefinition]
      summary: "Approve the current metric-definition draft (R2, owner only)"
      description: >-
        Approves the current draft version through the audited workspace-owner boundary. Only the
        owner may approve; the audit event is required, and audit failure leaves state unchanged. An
        approved version is immutable; a later change creates a new monotonically increasing
        version. The request body is empty: the author comes from the session, the version is the
        current draft, and time is server-owned.
      x-implementation-status: HANDLER_READY_NOT_COMPOSED
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          description: "Approved metric-definition version."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MetricDefinition"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/governed-query-connections/{connection_id}:set-live-queries:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
      - name: connection_id
        in: path
        required: true
        schema:
          $ref: "#/components/schemas/OpaqueID"
    post:
      operationId: setGovernedQueryLiveQueries
      tags: [Sources]
      summary: "Set whether live model queries are allowed for a connection (ADR-0089 section 6)"
      description: "Disabled by default. The body contains only the flag; DSN, credentials, and SQL are never accepted."
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [enabled]
              properties:
                enabled:
                  type: boolean
      responses:
        "200":
          description: "Connection and the current flag value."
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/governed-query-connections/{connection_id}/exposed-schema:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
      - name: connection_id
        in: path
        required: true
        schema:
          $ref: "#/components/schemas/OpaqueID"
    post:
      operationId: registerGovernedQueryExposedSchema
      tags: [Sources]
      summary: "Register an operator-exposed schema (ADR-0089 section 1)"
      description: >-
        The operator lists tables and columns exposed to the model. The server checks them against
        the dedicated role's information_schema and stores an immutable revision. Repeating the same
        schema converges on its revision.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [objects]
              properties:
                objects:
                  type: array
                  items:
                    type: object
      responses:
        "200":
          description: "Exposed-schema revision."
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/GovernedQueryUnavailable"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/governed-query-connections/{connection_id}:ask:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
      - name: connection_id
        in: path
        required: true
        schema:
          $ref: "#/components/schemas/OpaqueID"
    post:
      operationId: askGovernedQuery
      tags: [Search]
      summary: "Ask a natural-language question over the exposed schema (ADR-0089)"
      description: >-
        The model composes SQL against the exposed schema. A dedicated read-only role executes it
        with a timeout, row limit, and plan check. There is no SQL input field. The response
        contains the executed query, its hash, attempt ID, and complete result table. Values
        preserve their PostgreSQL text representation; SQL NULL is returned as JSON null. The
        execution interval is the query time, not the source record's modification time.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [question]
              properties:
                question:
                  type: string
                  minLength: 1
                  maxLength: 4000
      responses:
        "200":
          description: "Executed query, table, and result provenance."
          content:
            application/json:
              schema:
                type: object
                required: [connection_id, result]
                properties:
                  connection_id:
                    $ref: "#/components/schemas/OpaqueID"
                  result:
                    $ref: "#/components/schemas/GovernedQueryResult"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/GovernedQueryUnavailable"
        "422":
          $ref: "#/components/responses/GovernedQueryFailed"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /workspaces/{workspace_id}/governed-query-connections/{connection_id}:promote:
    parameters:
      - $ref: "#/components/parameters/WorkspaceID"
      - name: connection_id
        in: path
        required: true
        schema:
          $ref: "#/components/schemas/OpaqueID"
    post:
      operationId: promoteGovernedQueryAttempt
      tags: [Sources]
      summary: "Save an executed attempt as a verified projection (ADR-0089 section 5)"
      description: >-
        The body contains only a reference to the attempt and the hash the operator saw alongside
        the executed query; a SQL field is prohibited (PRODUCT_CONSTITUTION.md section 7). A hash
        mismatch with the retained attempt produces a content-free denial.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CSRFHeader"
        - $ref: "#/components/parameters/OriginHeader"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [attempt_id, sql_hash]
              properties:
                attempt_id:
                  $ref: "#/components/schemas/OpaqueID"
                sql_hash:
                  type: string
                  pattern: "^sha256:[0-9a-f]{64}$"
                name:
                  type: string
                  maxLength: 200
                schedule:
                  type: string
                  maxLength: 200
      responses:
        "200":
          description: "Save status, attempt identifier, and its hash."
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/CSRFRejected"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/GovernedQueryUnavailable"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /source-connectors:
    get:
      operationId: listSourceConnectors
      tags: [Sources]
      summary: "Versioned source-type catalog for onboarding"
      description: >-
        Only deterministic, content-free, read-only metadata for registration types (FOLDER,
        POSTGRESQL_QUERY, GIT). Excludes configured sources, credentials, raw DSNs, source-specific
        table/column names, database type fingerprints and hidden mounts. Authorization uses the
        same source-management OWNER policy as other source operations. An unauthenticated request
        returns 401; a policy denial returns the same content-free NOT_FOUND as other source reads
        without revealing configured sources. General discovery is not implemented. FOLDER and
        POSTGRESQL_QUERY have autonomous schedule-driven worker synchronization; GIT requires an
        explicitly refreshed checkout or provider. Supporting a type does not mean that a connection
        is configured or healthy.
      x-implementation-status: LIVE
      security:
        - SessionCookie: []
      responses:
        "200":
          description: "Versioned connector-type catalog (schema_version=1.0)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConnectorCatalogResponse"
        "400":
          $ref: "#/components/responses/RequestInvalid"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

components:
  securitySchemes:
    SessionCookie:
      type: apiKey
      in: cookie
      name: __Host-knowvault_session
      description: >-
        Server-issued, Secure, HttpOnly, host-only session cookie. OIDC
        issuance remains a separate authentication boundary.
    BearerSession:
      type: http
      scheme: bearer
      bearerFormat: opaque-session
      description: >-
        The same server-issued opaque session token may be sent once as
        Authorization: Bearer for non-browser API/MCP clients. Do not combine
        it with a Cookie header. This is not a long-lived service credential.
  parameters:
    WorkspaceID:
      name: workspace_id
      in: path
      required: true
      schema:
        $ref: "#/components/schemas/OpaqueID"
    PrincipalID:
      name: principal_id
      in: path
      required: true
      schema:
        $ref: "#/components/schemas/OpaqueID"
    SourceScopeID:
      name: source_scope_id
      in: path
      required: true
      schema:
        $ref: "#/components/schemas/OpaqueID"
    QuestionRunID:
      name: question_run_id
      in: path
      required: true
      schema:
        $ref: "#/components/schemas/OpaqueID"
    ConversationID:
      name: conversation_id
      in: path
      required: true
      schema:
        $ref: "#/components/schemas/OpaqueID"
    CSRFHeader:
      name: X-KnowVault-CSRF
      in: header
      required: true
      description: >-
        Exactly one derived CSRF proof from GET /session/csrf. Origin is also required and must
        exactly match the tenant origin; the handler checks it, but it is not a client-controlled
        tenant selector. These browser-only headers are omitted for Authorization: Bearer session
        transport.
      schema:
        type: string
        pattern: '^hmac-sha256:k[1-9][0-9]{0,8}:[0-9a-f]{64}$'
    OriginHeader:
      name: Origin
      in: header
      required: true
      description: >-
        Exactly one canonical HTTPS origin matching the tenant-bound server origin. Null,
        comma-separated, and cross-origin values are prohibited. Origin is omitted for
        Authorization: Bearer session transport.
      schema:
        type: string
        format: uri
        minLength: 1
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      description: >-
        Exactly one canonical 256-bit base64url value. A retry with the same body returns the
        immutable original result; a retry with a different body returns 409.
      schema:
        type: string
        pattern: '^[A-Za-z0-9_-]{43}$'
    IfMatch:
      name: If-Match
      in: header
      required: true
      description: >-
        Exactly one strong ETag matching the current workspace configuration. Weak tags, wildcards,
        and lists are prohibited.
      schema:
        type: string
        pattern: '^"sha256:[0-9a-f]{64}"$'
  headers:
    NoStore:
      description: "All handler JSON responses are noncacheable."
      required: true
      schema:
        type: string
        const: no-store
    RequestID:
      description: Server-generated correlation ID. Absent only if CSPRNG request-ID generation itself failed.
      required: false
      schema:
        type: string
        pattern: '^req_[A-Za-z0-9_-]{22}$'
    ETag:
      description: Strong configuration ETag exact returned Workspace snapshot.
      required: true
      schema:
        type: string
        pattern: '^"sha256:[0-9a-f]{64}"$'
    NoSniff:
      description: Browser content-type sniffing is disabled.
      required: true
      schema:
        type: string
        const: nosniff
  schemas:
    GovernedQueryResult:
      type: object
      additionalProperties: false
      required: [attempt_id, sql, sql_hash, columns, rows, row_count, cost_estimate, answer, connection_id, database_identity, exposed_schema_revision, result_format, result_digest, execution_started_at, execution_completed_at]
      properties:
        attempt_id:
          type: string
        sql:
          type: string
        sql_hash:
          type: string
        columns:
          type: array
          items:
            type: string
        rows:
          type: array
          items:
            type: array
            items:
              type: [string, "null"]
        row_count:
          type: integer
          minimum: 0
        cost_estimate:
          type: number
        answer:
          type: string
        connection_id:
          $ref: "#/components/schemas/OpaqueID"
        database_identity:
          type: string
        exposed_schema_revision:
          type: integer
          minimum: 1
        result_format:
          type: string
          const: postgres-text-table-v1
        result_digest:
          type: string
          description: SHA-256 of canonical JSON with format, columns, row_count and rows, including values and NULLs.
        execution_started_at:
          type: string
          format: date-time
        execution_completed_at:
          type: string
          format: date-time
    HealthResponse:
      type: object
      additionalProperties: false
      required: [status, component]
      properties:
        status:
          type: string
          enum: [UP]
        component:
          type: string
          enum: [knowvault-server]
    BuildInfoResponse:
      type: object
      additionalProperties: false
      required: [version, revision, built_at, go_version, go_experiment]
      properties:
        version:
          type: string
          minLength: 1
        revision:
          type: string
          minLength: 1
        built_at:
          type: string
          minLength: 1
        go_version:
          type: string
          minLength: 1
        go_experiment:
          type: string
          minLength: 1
    SystemCapabilitiesResponse:
      type: object
      additionalProperties: false
      required: [schema_version, component, release_eligible, capabilities]
      properties:
        schema_version:
          type: string
          const: system-capabilities-v1
        component:
          type: string
          const: knowvault-server
        release_eligible:
          type: boolean
          const: false
        capabilities:
          type: array
          minItems: 1
          maxItems: 64
          items:
            $ref: "#/components/schemas/SystemCapability"
    SystemCapability:
      type: object
      additionalProperties: false
      required: [id, status]
      properties:
        id:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[a-z][a-z0-9_]{0,127}$'
        status:
          type: string
          enum: [READY, PARTIAL, BLOCKED, UNAVAILABLE]
        reason_code:
          type: string
          minLength: 3
          maxLength: 64
          pattern: '^[A-Z0-9_]{3,64}$'
    OpaqueID:
      type: string
      minLength: 3
      maxLength: 128
      pattern: '^[A-Za-z0-9_.:-]{3,128}$'
    EvidenceFragmentNeighbor:
      type: object
      additionalProperties: false
      required: [fragment_id, ordinal, canonical_address]
      properties:
        fragment_id:
          type: string
        ordinal:
          type: integer
          format: int64
        canonical_address:
          type: string
    SourceDiscoveryResponse:
      type: object
      additionalProperties: false
      required:
        - request_id
        - request_status
        - view_count
        - prepared_view_count
        - needs_interpretation_view_count
        - expires_at
        - views
      properties:
        request_id:
          type: string
          pattern: '^sdrq_[0-7][0-9A-HJKMNP-TV-Z]{25}$'
        request_status:
          type: string
          enum: [PENDING, RUNNING, SUCCEEDED, FAILED, EXPIRED]
        result_id:
          type: string
          pattern: '^sdr_[0-7][0-9A-HJKMNP-TV-Z]{25}$'
        result_status:
          type: string
          enum: [SUCCEEDED, NEEDS_INTERPRETATION]
        view_count:
          type: integer
          minimum: 0
          maximum: 64
        prepared_view_count:
          type: integer
          minimum: 0
          maximum: 64
        needs_interpretation_view_count:
          type: integer
          minimum: 0
          maximum: 64
        failure_code:
          type: string
          minLength: 1
          maxLength: 64
        expires_at:
          type: string
          format: date-time
        views:
          type: array
          maxItems: 64
          items:
            $ref: "#/components/schemas/SourceDiscoveryView"
    SourceDiscoveryView:
      type: object
      additionalProperties: false
      required: [view_id, schema_name, relation_name, relation_kind, status, columns]
      properties:
        view_id:
          type: string
          pattern: '^sdv_[0-9a-f]{64}$'
        schema_name:
          type: string
          minLength: 1
          maxLength: 63
        relation_name:
          type: string
          minLength: 1
          maxLength: 63
        relation_kind:
          type: string
          enum: [VIEW, MATERIALIZED_VIEW]
        comment:
          type: string
          maxLength: 4096
        status:
          type: string
          enum: [PREPARED, NEEDS_INTERPRETATION]
        interpretation:
          type: string
          enum:
            - UNRECOGNIZED_FORMAT
            - INCOMPLETE_BUSINESS_OBJECT_CONTRACT
            - MALFORMED_BUSINESS_OBJECT_CONTRACT
            - UNSUPPORTED_TYPE
            - INVALID_IDENTIFIER
        columns:
          type: array
          maxItems: 256
          items:
            $ref: "#/components/schemas/SourceDiscoveryColumn"
    SourceDiscoveryColumn:
      type: object
      additionalProperties: false
      required: [ordinal, name, type_name, logical_type, nullable, roles]
      properties:
        ordinal:
          type: integer
          minimum: 1
          maximum: 256
        name:
          type: string
          minLength: 1
          maxLength: 63
        type_name:
          type: string
          minLength: 1
          maxLength: 256
        logical_type:
          type: string
          enum: [BOOL, INT, NUMERIC, UUID, DATE, TIMESTAMP, TIMESTAMPTZ, TEXT, JSON, JSONB]
        nullable:
          type: boolean
        precision:
          type: integer
          minimum: 0
        scale:
          type: integer
          minimum: 0
        max_bytes:
          type: integer
          minimum: 0
        comment:
          type: string
          maxLength: 4096
        roles:
          type: array
          uniqueItems: true
          maxItems: 6
          items:
            type: string
            enum: [IDENTITY, TITLE, VERSION_HINT, EVIDENCE, PERIOD, STATUS]
    WorkspaceRole:
      type: string
      enum: [OWNER, MANAGER, MEMBER, VIEWER, AUDITOR]
    AssignableWorkspaceRole:
      type: string
      enum: [MANAGER, MEMBER, VIEWER, AUDITOR]
    WorkspaceStatus:
      type: string
      enum: [ACTIVE, READ_ONLY, ARCHIVED, DELETING, DELETED]
    WorkspaceMetadataInput:
      type: object
      additionalProperties: false
      required: [name, description, retention_policy_id]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 256
        description:
          type: string
          maxLength: 4096
        retention_policy_id:
          type: string
          maxLength: 128
          pattern: '^(|[A-Za-z0-9_.:-]{3,128})$'
    AddMemberInput:
      type: object
      additionalProperties: false
      required: [principal_id, role]
      properties:
        principal_id:
          $ref: "#/components/schemas/OpaqueID"
        role:
          $ref: "#/components/schemas/AssignableWorkspaceRole"
    MemberRoleInput:
      type: object
      additionalProperties: false
      required: [role]
      properties:
        role:
          $ref: "#/components/schemas/AssignableWorkspaceRole"
    TransferOwnershipInput:
      type: object
      additionalProperties: false
      required: [new_owner_principal_id]
      properties:
        new_owner_principal_id:
          $ref: "#/components/schemas/OpaqueID"
    CSRFTokenResponse:
      type: object
      additionalProperties: false
      required: [csrf_token]
      properties:
        csrf_token:
          type: string
          pattern: '^hmac-sha256:k[1-9][0-9]{0,8}:[0-9a-f]{64}$'
    WorkspaceMember:
      type: object
      additionalProperties: false
      required: [principal_id, display_name, role]
      properties:
        principal_id:
          $ref: "#/components/schemas/OpaqueID"
        display_name:
          description: Same-organization principal display name; email and external identity are not returned.
          type: string
          minLength: 1
          maxLength: 256
        role:
          $ref: "#/components/schemas/WorkspaceRole"
    MemberCandidate:
      type: object
      additionalProperties: false
      required: [principal_id, display_name, already_member]
      properties:
        principal_id:
          $ref: "#/components/schemas/OpaqueID"
        display_name:
          type: string
          minLength: 1
          maxLength: 256
        already_member:
          type: boolean
    MetricDefinition:
      type: object
      additionalProperties: false
      required:
        - id
        - version
        - status
        - name
        - source_connection_id
        - projection_version
        - entity_key
        - grain
        - allowed_filters
        - unit
      properties:
        id:
          $ref: "#/components/schemas/OpaqueID"
        version:
          type: integer
          minimum: 1
          maximum: 9007199254740991
        status:
          type: string
          enum: [DRAFT, APPROVED, RETIRED]
        name:
          type: string
          minLength: 1
          maxLength: 200
        source_connection_id:
          $ref: "#/components/schemas/OpaqueID"
        projection_version:
          type: integer
          minimum: 1
          maximum: 9007199254740991
        entity_key:
          type: string
          minLength: 1
          maxLength: 256
        grain:
          type: string
          enum: [DAY, WEEK, MONTH, QUARTER, YEAR]
        allowed_filters:
          type: array
          maxItems: 64
          items:
            type: string
            minLength: 1
            maxLength: 128
        unit:
          type: string
          maxLength: 64
    MetricDefinitionDraftRequest:
      type: object
      additionalProperties: false
      required: [id, name, source_connection_id, projection_version, entity_key, grain]
      properties:
        id:
          $ref: "#/components/schemas/OpaqueID"
        name:
          type: string
          minLength: 1
          maxLength: 200
        source_connection_id:
          $ref: "#/components/schemas/OpaqueID"
        projection_version:
          type: integer
          minimum: 1
          maximum: 9007199254740991
        entity_key:
          type: string
          minLength: 1
          maxLength: 256
        grain:
          type: string
          enum: [DAY, WEEK, MONTH, QUARTER, YEAR]
        allowed_filters:
          type: array
          maxItems: 64
          items:
            type: string
            minLength: 1
            maxLength: 128
        unit:
          type: string
          maxLength: 64
    Workspace:
      type: object
      additionalProperties: false
      required:
        [id, name, description, status, revision, owner_principal_id, retention_policy_id, members, processing_mode]
      properties:
        id:
          $ref: "#/components/schemas/OpaqueID"
        name:
          type: string
          minLength: 1
          maxLength: 256
        description:
          type: string
          maxLength: 4096
        status:
          $ref: "#/components/schemas/WorkspaceStatus"
        revision:
          type: integer
          minimum: 1
          maximum: 9007199254740991
        owner_principal_id:
          $ref: "#/components/schemas/OpaqueID"
        retention_policy_id:
          type: string
          maxLength: 128
          pattern: '^(|[A-Za-z0-9_.:-]{3,128})$'
        members:
          type: array
          items:
            $ref: "#/components/schemas/WorkspaceMember"
        processing_mode:
          description: >-
            Processing location of the default generation profile, not a
            per-request selection. INTERNAL_UNAVAILABLE when the default is
            unavailable in this workspace. A completed tool-loop answer may
            carry its actual selected model_profile separately.
          type: object
          additionalProperties: false
          required: [mode]
          properties:
            mode:
              type: string
              enum: [INTERNAL, EXTERNAL, INTERNAL_UNAVAILABLE]
            provider:
              type: string
              maxLength: 256
        model_profiles:
          description: >-
            Mounted tool-loop profiles available in this currently authorized
            workspace, filtered by the server's exact workspace allow-lists.
            Contains no endpoint, credential, or mount path. Empty when none
            is available; a caller cannot widen this catalogue with a profile ID.
          type: array
          items:
            $ref: "#/components/schemas/QuestionModelProfileOption"
    WorkspaceSummary:
      type: object
      additionalProperties: false
      required: [id, name, status, revision, role]
      properties:
        id:
          $ref: "#/components/schemas/OpaqueID"
        name:
          type: string
          minLength: 1
          maxLength: 256
        status:
          $ref: "#/components/schemas/WorkspaceStatus"
        revision:
          type: integer
          minimum: 1
          maximum: 9007199254740991
        role:
          $ref: "#/components/schemas/WorkspaceRole"
        degraded:
          type: boolean
        degraded_reason:
          type: string
          enum: [WORKSPACE_CONFIGURATION_HASH_STALE]
    WorkspaceList:
      type: object
      additionalProperties: false
      required: [workspaces]
      properties:
        workspaces:
          type: array
          items:
            $ref: "#/components/schemas/WorkspaceSummary"
    ErrorResponse:
      type: object
      additionalProperties: false
      required: [error]
      properties:
        error:
          type: object
          additionalProperties: false
          required: [code, request_id]
          properties:
            code:
              type: string
              enum:
                [REQUEST_INVALID, UNAUTHENTICATED, CSRF_REJECTED, FORBIDDEN,
                 NOT_FOUND, METHOD_NOT_ALLOWED, PRECONDITION_REQUIRED,
                 WORKSPACE_REVISION_CONFLICT, WORKSPACE_IDEMPOTENCY_CONFLICT,
                 QUESTION_IDEMPOTENCY_CONFLICT, CONVERSATION_IDEMPOTENCY_CONFLICT,
                 SOURCE_CONFLICT, SOURCE_UNAVAILABLE,
                 PAYLOAD_TOO_LARGE, UNSUPPORTED_MEDIA_TYPE, SERVICE_UNAVAILABLE]
            request_id:
              type: string
              maxLength: 128
            fields:
              description: >-
                FIX-7 #2: present only for a validation failure at this HTTP
                surface (typically code=REQUEST_INVALID, also
                PRECONDITION_REQUIRED/PAYLOAD_TOO_LARGE/UNSUPPORTED_MEDIA_TYPE):
                the exact request field or header NAMES that failed
                validation, never their values. The same rejection is also
                logged server-side as a WARN carrying request_id, the route
                and this same field list, so a request_id from a client is
                enough to find and understand the rejection in the server's
                own logs. Absent for every other error and for a validation
                failure not attributable to a named field (an unparseable
                body, an unsupported Content-Type).
              type: array
              items:
                type: string
                maxLength: 128
              maxItems: 32
            clarification:
              description: >-
                R2 Outcome 2 (KV-A02): present only when a typed QueryIntent
                refusal carries a server-owned clarification for a human. It is
                the closed-dictionary clarification text the server emits,
                never a client-supplied value; clients should show it to the
                user. Absent for every other error, so every pre-R2 error body
                keeps its exact shape.
              type: string
              maxLength: 2048
    RelativePath:
      type: string
      minLength: 1
      maxLength: 4096
      pattern: '^(?![/\\])(?!.*(?:^|[/\\])\.\.?(?:[/\\]|$))(?!.*[\u0000-\u001F\u007F]).*$'
    PathPattern:
      type: string
      minLength: 1
      maxLength: 4096
      pattern: '^(?![/\\])(?!.*(?:^|[/\\])\.\.?(?:[/\\]|$))(?!.*[\u0000-\u001F\u007F]).*$'
    GlobList:
      type: array
      maxItems: 128
      items:
        $ref: "#/components/schemas/PathPattern"
    SourceRegisterInput:
      oneOf:
        - $ref: "#/components/schemas/FolderSourceRegisterInput"
        - $ref: "#/components/schemas/GitSourceRegisterInput"
        - $ref: "#/components/schemas/MailSourceRegisterInput"
        - $ref: "#/components/schemas/PostgreSQLQuerySourceRegisterInput"
    ConnectorCatalogResponse:
      type: object
      additionalProperties: false
      required: [schema_version, declarations, connectors]
      properties:
        schema_version:
          type: string
          const: "1.0"
        declarations:
          $ref: "#/components/schemas/ConnectorCatalogDeclarations"
        connectors:
          type: array
          items:
            $ref: "#/components/schemas/SourceConnectorDefinition"
    ConnectorCatalogDeclarations:
      type: object
      additionalProperties: false
      required: [general_discovery_implemented, autonomous_recurring_sync_implemented, autonomous_recurring_sync_source_types]
      properties:
        general_discovery_implemented:
          type: boolean
          const: false
        autonomous_recurring_sync_implemented:
          type: boolean
          const: true
        autonomous_recurring_sync_source_types:
          type: array
          items:
            type: string
            enum: [FOLDER, POSTGRESQL_QUERY]
    SourceConnectorDefinition:
      type: object
      additionalProperties: false
      required: [type, label, description, registration_fields, capabilities]
      properties:
        type:
          type: string
          enum: [FOLDER, POSTGRESQL_QUERY, GIT]
        label:
          type: string
        description:
          type: string
        registration_fields:
          type: array
          items:
            $ref: "#/components/schemas/ConnectorRegistrationField"
        capabilities:
          $ref: "#/components/schemas/ConnectorCapabilities"
    ConnectorRegistrationField:
      type: object
      additionalProperties: false
      required: [name, required, description]
      properties:
        name:
          type: string
        required:
          type: boolean
        description:
          type: string
    ConnectorCapabilities:
      type: object
      additionalProperties: false
      required: [autonomous_recurring_sync, general_discovery]
      properties:
        autonomous_recurring_sync:
          type: boolean
        general_discovery:
          type: boolean
          const: false
    FolderSourceRegisterInput:
      type: object
      additionalProperties: false
      required: [name, root_alias, root_identity, relative_root, kind, recursive, include_globs, exclude_globs, max_file_bytes, ocr_mode, formats]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 128
        root_alias:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]*$'
        root_identity:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[A-Za-z0-9][A-Za-z0-9._:-]*$'
        relative_root:
          $ref: "#/components/schemas/RelativePath"
        kind:
          type: string
          minLength: 1
          maxLength: 64
        recursive:
          type: boolean
        include_globs:
          $ref: "#/components/schemas/GlobList"
        exclude_globs:
          $ref: "#/components/schemas/GlobList"
        max_file_bytes:
          type: integer
          minimum: 1
          maximum: 1073741824
        ocr_mode:
          type: string
          enum: [OFF, AUTO]
        formats:
          type: array
          minItems: 1
          maxItems: 14
          uniqueItems: true
          items:
            type: string
            enum: [PDF, DOCX, PPTX, XLSX, CSV, TXT, MARKDOWN, HTML, JSON, XML, EML, SOURCE_CODE, PNG, JPEG]
    GitSourceRegisterInput:
      type: object
      additionalProperties: false
      required: [source_type, name, kind, provider, endpoint, repository_id, branch_name, include_globs, exclude_globs, text_media_types, max_blob_bytes]
      properties:
        source_type:
          type: string
          const: GIT
        name:
          type: string
          minLength: 1
          maxLength: 128
        kind:
          type: string
          minLength: 1
          maxLength: 64
        provider:
          type: string
          enum: [GITHUB, GITLAB]
        endpoint:
          type: string
          format: uri
          pattern: '^https://'
          maxLength: 512
        web_base_url:
          type: string
          format: uri
          pattern: '^https://'
          maxLength: 512
        credential_reference:
          type: string
          description: Opaque read-only source credential reference; secret bytes never enter the API.
          pattern: '^cred_[0-7][0-9A-HJKMNP-TV-Z]{25}$'
        repository_id:
          type: string
          minLength: 1
          maxLength: 512
        branch_name:
          type: string
          minLength: 1
          maxLength: 256
        include_globs:
          $ref: "#/components/schemas/GlobList"
        exclude_globs:
          $ref: "#/components/schemas/GlobList"
        text_media_types:
          type: array
          minItems: 1
          maxItems: 128
          uniqueItems: true
          items:
            type: string
            minLength: 3
            maxLength: 128
        max_blob_bytes:
          type: integer
          minimum: 1
          maximum: 1073741824
    MailSourceRegisterInput:
      type: object
      additionalProperties: false
      required: [source_type, name, kind, endpoint, mailbox, folder, username, include_attachments]
      properties:
        source_type:
          type: string
          const: MAIL
        name:
          type: string
          minLength: 1
          maxLength: 128
        kind:
          type: string
          minLength: 1
          maxLength: 64
        provider:
          type: string
          const: IMAP
        endpoint:
          type: string
          description: TLS IMAP host:port; the scheme is intentionally absent.
          pattern: '^[A-Za-z0-9.-]+:[0-9]{1,5}$'
          maxLength: 512
        credential_reference:
          type: string
          description: Opaque read-only source credential reference; secret bytes never enter the API.
          pattern: '^cred_[0-7][0-9A-HJKMNP-TV-Z]{25}$'
        mailbox:
          type: string
          minLength: 1
          maxLength: 96
        folder:
          type: string
          minLength: 1
          maxLength: 96
        username:
          type: string
          minLength: 1
          maxLength: 256
        since:
          type: string
          format: date-time
        include_attachments:
          type: boolean
        max_message_bytes:
          type: integer
          minimum: 1
          maximum: 67108864
        max_attachment_bytes:
          type: integer
          minimum: 1
          maximum: 33554432
        attachment_media_types:
          type: array
          maxItems: 128
          uniqueItems: true
          items:
            type: string
            minLength: 3
            maxLength: 128
    PostgreSQLQuerySourceRegisterInput:
      type: object
      additionalProperties: false
      required: [source_type, name, kind, database_identity, lineage_id, projection_revision, contract_hash, schema_name, relation_name, relation_kind, columns, empty_snapshot_policy]
      properties:
        source_type:
          type: string
          const: POSTGRESQL_QUERY
        name:
          type: string
          minLength: 1
          maxLength: 128
        kind:
          type: string
          minLength: 1
          maxLength: 64
        database_identity:
          type: string
          minLength: 1
          maxLength: 128
        credential_reference:
          type: string
          description: Opaque worker-mounted credential reference; secret bytes never enter the API.
          pattern: '^cred_[0-7][0-9A-HJKMNP-TV-Z]{25}$'
        lineage_id:
          type: string
          minLength: 1
          maxLength: 128
        projection_revision:
          type: integer
          minimum: 1
        contract_hash:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        schema_name:
          type: string
          pattern: '^[A-Za-z_][A-Za-z0-9_$]{0,62}$'
        relation_name:
          type: string
          pattern: '^[A-Za-z_][A-Za-z0-9_$]{0,62}$'
        relation_kind:
          type: string
          enum: [VIEW, MATERIALIZED_VIEW]
        columns:
          type: array
          minItems: 1
          maxItems: 128
          items:
            type: object
            additionalProperties: false
            required: [ordinal, name, type_fingerprint, logical_type, roles, max_bytes]
            properties:
              ordinal: {type: integer, minimum: 1}
              name: {type: string, pattern: '^[A-Za-z_][A-Za-z0-9_$]{0,62}$'}
              type_fingerprint: {type: string, minLength: 1, maxLength: 128}
              logical_type:
                type: string
                enum: [BOOL, INT, NUMERIC, UUID, DATE, TIMESTAMP, TIMESTAMPTZ, TEXT, JSON, JSONB]
              roles:
                type: array
                minItems: 1
                uniqueItems: true
                items: {type: string, enum: [IDENTITY, TITLE, VERSION_HINT, EVIDENCE, PERIOD, STATUS]}
              nullable: {type: boolean}
              precision: {type: integer, minimum: 0, maximum: 1000}
              scale: {type: integer, minimum: 0, maximum: 1000}
              max_bytes: {type: integer, minimum: 1, maximum: 67108864}
        empty_snapshot_policy:
          type: string
          enum: [HELD, AUTHORITATIVE]
        max_rows: {type: integer, minimum: 1, maximum: 10000000}
        max_columns: {type: integer, minimum: 1, maximum: 128}
        max_field_bytes: {type: integer, minimum: 1, maximum: 1048576}
        max_row_bytes: {type: integer, minimum: 1, maximum: 16777216}
        max_total_bytes: {type: integer, minimum: 1, maximum: 1073741824}
        statement_timeout_ms: {type: integer, minimum: 100, maximum: 300000}
        sync_interval_seconds: {type: integer, minimum: 60, maximum: 86400}
    SourceRegisterResult:
      type: object
      additionalProperties: false
      required: [connection_id, source_scope_id, discovered_scope_id, revision, scope_config_hash, access_mode, created]
      properties:
        connection_id:
          $ref: "#/components/schemas/OpaqueID"
        credential_reference:
          type: string
          description: >-
            Opaque source-mount reference for POSTGRESQL_QUERY registrations;
            never contains credential bytes and is omitted for FOLDER.
          pattern: '^cred_[0-7][0-9A-HJKMNP-TV-Z]{25}$'
        source_scope_id:
          $ref: "#/components/schemas/OpaqueID"
        discovered_scope_id:
          $ref: "#/components/schemas/OpaqueID"
        revision:
          type: integer
          minimum: 1
        scope_config_hash:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        access_mode:
          type: string
          enum: [WORKSPACE_MANAGED]
        created:
          type: boolean
    SourceActivateResult:
      type: object
      additionalProperties: false
      required: [job_id]
      properties:
        job_id:
          $ref: "#/components/schemas/OpaqueID"
    SourceStatus:
      type: object
      additionalProperties: false
      required: [workspace_source_id, source_scope_id, source_scope_revision, access_mode, enabled, scope_config_hash, connection_id, connection_name, source_type, postgresql_schema_name, postgresql_relation_name, activation_status, trust_verified, content_freshness_sla_seconds, freshness_state]
      properties:
        workspace_source_id:
          $ref: "#/components/schemas/OpaqueID"
        source_scope_id:
          $ref: "#/components/schemas/OpaqueID"
        source_scope_revision:
          type: integer
          minimum: 1
        access_mode:
          type: string
          enum: [WORKSPACE_MANAGED]
        enabled:
          type: boolean
        scope_config_hash:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        connection_id:
          $ref: "#/components/schemas/OpaqueID"
        connection_name:
          type: string
          minLength: 1
          maxLength: 128
        source_type:
          type: string
          enum: [FOLDER, GIT, MAIL, SITE, POSTGRESQL_QUERY]
        postgresql_schema_name:
          type: string
          nullable: true
          pattern: '^[A-Za-z_][A-Za-z0-9_$]{0,62}$'
          description: Schema of the prepared PostgreSQL projection for this exact workspace source revision; null for other source types.
        postgresql_relation_name:
          type: string
          nullable: true
          pattern: '^[A-Za-z_][A-Za-z0-9_$]{0,62}$'
          description: Relation of the prepared PostgreSQL projection for this exact workspace source revision; null for other source types.
        activation_status:
          type: string
          enum: [DRAFT, SYNCING, READY, REVOKED, FAILED]
        trust_verified:
          type: boolean
        sync_status:
          type: string
          nullable: true
          enum: [RUNNING, SUCCEEDED, FAILED]
        sync_error_code:
          type: string
          nullable: true
        sync_started_at:
          type: string
          format: date-time
          nullable: true
        sync_completed_at:
          type: string
          format: date-time
          nullable: true
        objects_seen:
          type: integer
          minimum: 0
          nullable: true
        objects_ingested:
          type: integer
          minimum: 0
          nullable: true
        versions_created:
          type: integer
          minimum: 0
          nullable: true
        evidence_published:
          type: integer
          minimum: 0
          nullable: true
        quarantined:
          type: integer
          minimum: 0
          nullable: true
        job_id:
          $ref: "#/components/schemas/OpaqueID"
          nullable: true
        job_status:
          type: string
          nullable: true
          enum: [PENDING, RUNNING, SUCCEEDED, DEAD]
        job_attempt_count:
          type: integer
          minimum: 0
          nullable: true
        job_max_attempts:
          type: integer
          minimum: 1
          nullable: true
        job_available_at:
          type: string
          format: date-time
          nullable: true
        job_lease_expires_at:
          type: string
          format: date-time
          nullable: true
        job_last_error_code:
          type: string
          nullable: true
        content_freshness_sla_seconds:
          type: integer
          minimum: 60
          maximum: 604800
        last_successful_sync_at:
          type: string
          format: date-time
          nullable: true
        freshness_state:
          type: string
          enum: [UNKNOWN, FRESH, STALE]
    SourceList:
      type: object
      additionalProperties: false
      required: [sources]
      properties:
        sources:
          type: array
          items:
            $ref: "#/components/schemas/SourceStatus"
    WorkspaceSourceBindInput:
      type: object
      additionalProperties: false
      required: [expected_workspace_revision, source_scope_id, source_scope_revision, scope_config_hash, access_mode]
      properties:
        expected_workspace_revision:
          type: integer
          minimum: 1
        source_scope_id:
          $ref: "#/components/schemas/OpaqueID"
        source_scope_revision:
          type: integer
          minimum: 1
        scope_config_hash:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        access_mode:
          type: string
          enum: [WORKSPACE_MANAGED, SOURCE_ENFORCED]
    WorkspaceSourceRemoveInput:
      type: object
      additionalProperties: false
      required: [expected_workspace_revision, workspace_source_id, source_scope_id, source_scope_revision, scope_config_hash, access_mode]
      properties:
        expected_workspace_revision:
          type: integer
          minimum: 1
        workspace_source_id:
          $ref: "#/components/schemas/OpaqueID"
        source_scope_id:
          $ref: "#/components/schemas/OpaqueID"
        source_scope_revision:
          type: integer
          minimum: 1
        scope_config_hash:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        access_mode:
          type: string
          enum: [WORKSPACE_MANAGED, SOURCE_ENFORCED]
    QuestionModelProfileID:
      type: string
      minLength: 1
      maxLength: 64
      pattern: '^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$'
    QuestionModelProfile:
      type: object
      additionalProperties: false
      required: [id, label, location]
      properties:
        id:
          $ref: "#/components/schemas/QuestionModelProfileID"
        label:
          type: string
          minLength: 1
          maxLength: 80
        location:
          type: string
          enum: [INTERNAL, EXTERNAL]
    QuestionModelProfileOption:
      type: object
      additionalProperties: false
      required: [id, label, location, is_default]
      properties:
        id:
          $ref: "#/components/schemas/QuestionModelProfileID"
        label:
          type: string
          minLength: 1
          maxLength: 80
        location:
          type: string
          enum: [INTERNAL, EXTERNAL]
        is_default:
          type: boolean
    QuestionCreateInput:
      type: object
      additionalProperties: false
      required: [question]
      properties:
        question:
          type: string
          minLength: 1
          maxLength: 32768
        conversation_id:
          description: Existing server-owned conversation to continue; omitted to create a new conversation.
          $ref: "#/components/schemas/OpaqueID"
        model_profile_id:
          description: >-
            Optional ID from this workspace's model_profiles catalogue. Selects
            one immutable adapter for this TOOL_LOOP request; omitted answer_mode
            becomes TOOL_LOOP. Explicit EXTRACTIVE or GENERATIVE with this field
            is invalid. Unknown or unavailable profiles fail without fallback.
            Reusing an idempotency key with a different explicit profile conflicts.
            Omission preserves the legacy default and replay identity.
          $ref: "#/components/schemas/QuestionModelProfileID"
        answer_mode:
          type: string
          description: >-
            Omission selects TOOL_LOOP whenever the server has composed the shared tool runtime; a
            composition without that runtime defaults to EXTRACTIVE. TOOL_LOOP additionally requires
            a generation profile available to this workspace and fails with
            QUESTION_MODE_UNSUPPORTED if none is available, without falling back to EXTRACTIVE. It
            uses the shared workspace knowledge tools and records address-bound citations and tool
            calls. GENERATIVE is a compatibility mode, available only when an explicit Model Gateway
            generation mount is wired for the caller's workspace (GEN-2, ADR-0088). A GENERATIVE
            request without that mount is rejected with QUESTION_MODE_UNSUPPORTED before any run is
            created.
          enum: [EXTRACTIVE, GENERATIVE, TOOL_LOOP]
    ConversationTurn:
      type: object
      additionalProperties: false
      required: [turn_id, question_run_id, turn_index, created_at]
      properties:
        turn_id:
          $ref: "#/components/schemas/OpaqueID"
        question_run_id:
          $ref: "#/components/schemas/OpaqueID"
        turn_index:
          type: integer
          minimum: 1
          maximum: 9007199254740991
        created_at:
          type: string
          format: date-time
        question_run:
          description: Current-access-checked Question Run projection; omitted when a concurrent revocation removes disclosure.
          allOf:
            - $ref: "#/components/schemas/QuestionRun"
    Conversation:
      type: object
      additionalProperties: false
      required: [conversation_id, workspace_id, workspace_revision, created_by, created_at, turns]
      properties:
        conversation_id:
          $ref: "#/components/schemas/OpaqueID"
        workspace_id:
          $ref: "#/components/schemas/OpaqueID"
        workspace_revision:
          type: integer
          minimum: 1
          maximum: 9007199254740991
        created_by:
          $ref: "#/components/schemas/OpaqueID"
        created_at:
          type: string
          format: date-time
        archived_at:
          type: string
          format: date-time
        turns:
          type: array
          maxItems: 500
          items:
            $ref: "#/components/schemas/ConversationTurn"
    ConversationList:
      type: object
      additionalProperties: false
      required: [conversations]
      properties:
        conversations:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/Conversation"
        next_cursor:
          type: string
          minLength: 3
          maxLength: 128
          pattern: '^[A-Za-z0-9_.:-]{3,128}$'
          description: >-
            Opaque conversation_id of the last returned thread. Present only when another page
            exists; the client returns it unchanged in cursor.
    QuestionCitation:
      type: object
      additionalProperties: false
      required: [number, citation_id, evidence_fragment_id, excerpt, anchor, deep_link, source_version_id, extraction_id, source_object_id, evidence_text_hash, excerpt_hash]
      properties:
        address:
          type: string
          description: Canonical address returned by a knowledge tool and read back before binding this quote.
          pattern: '^kv1:'
        number:
          type: integer
          minimum: 1
        citation_id:
          $ref: "#/components/schemas/OpaqueID"
        evidence_fragment_id:
          $ref: "#/components/schemas/OpaqueID"
        excerpt:
          type: string
          minLength: 1
          maxLength: 32768
        anchor:
          type: string
          minLength: 1
          maxLength: 32768
        deep_link:
          type: string
          minLength: 1
          maxLength: 4096
        source_version_id:
          $ref: "#/components/schemas/OpaqueID"
        extraction_id:
          $ref: "#/components/schemas/OpaqueID"
        source_object_id:
          $ref: "#/components/schemas/OpaqueID"
        evidence_text_hash:
          type: string
          description: Organization-scoped HMAC digest copied from the authorized Evidence fragment; plain SHA is never exposed.
          pattern: '^hmac-sha256:k[1-9][0-9]{0,8}:[0-9a-f]{64}$'
        excerpt_hash:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        source_quote:
          description: >-
            SOURCE_QUOTE binding: present only when the citation is bound to an
            exact span of the stored extraction. Absent (or unbound) citations
            remain readable and are labelled UNCONFIRMED.
          $ref: "#/components/schemas/SourceQuote"
        grounding_status:
          type: string
          description: >-
            Per-citation R1 grounding state exposed by REST and MCP. CONFIRMED_BY_FRAGMENT
            ("confirmed by fragment") applies only when source_quote is present, its span [start,
            end) lies inside the retained extraction's normalized text, and text_hash hashes exactly
            that span text. Everything else, including a paraphrase supported only by embedding
            similarity, is UNCONFIRMED ("unconfirmed"); similarity is never proof.
          enum: [CONFIRMED_BY_FRAGMENT, UNCONFIRMED]
          default: UNCONFIRMED
    SourceQuote:
      type: object
      additionalProperties: false
      required: [source_version_id, extraction_id, anchor, start, end, text_hash]
      description: >-
        SOURCE_QUOTE = {source_version_id, extraction_id, anchor, start, end,
        text_hash}. start/end are 0-based character offsets into the
        extraction's normalized text (end exclusive); text_hash is the hash of
        exactly that span. A citation whose span hash does not match the stored
        extraction is unbound (UNCONFIRMED).
      properties:
        source_version_id:
          $ref: "#/components/schemas/OpaqueID"
        extraction_id:
          $ref: "#/components/schemas/OpaqueID"
        anchor:
          type: string
          minLength: 1
          maxLength: 32768
        start:
          type: integer
          minimum: 0
        end:
          type: integer
          minimum: 0
        text_hash:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
    QuestionUncertainty:
      type: object
      additionalProperties: false
      required: [code, evidence_ids]
      properties:
        code:
          type: string
          pattern: '^[A-Z][A-Z0-9_]{2,63}$'
        evidence_ids:
          type: array
          maxItems: 256
          items:
            $ref: "#/components/schemas/OpaqueID"
        message:
          description: >-
            Server-owned, closed-vocabulary English sentence derived solely from code (FIX-2 #6).
            Recomputed on every read; never persisted.
          type: string
          maxLength: 512
    QuestionConflict:
      type: object
      additionalProperties: false
      required: [code, evidence_ids]
      properties:
        code:
          type: string
          pattern: '^[A-Z][A-Z0-9_]{2,63}$'
        evidence_ids:
          type: array
          minItems: 2
          maxItems: 256
          items:
            $ref: "#/components/schemas/OpaqueID"
        message:
          description: >-
            Server-owned, closed-vocabulary English sentence derived solely from code (FIX-2 #6).
          type: string
          maxLength: 512
    QuestionRun:
      type: object
      additionalProperties: false
      required: [question_run_id, workspace_id, workspace_revision, question, answer_mode, verification_method, status, corpus_status, freshness, started_at, manifest_status, planning_status, planning_operation, planning_confidence, citations, uncertainties, conflicts]
      properties:
        tool_loop:
          $ref: "#/components/schemas/QuestionToolLoop"
        model_profile:
          description: >-
            Actual selection captured in the encrypted terminal tool-loop trace,
            including its label and location at execution time. Not resolved from
            current configuration. Absent for older traces and when a run failed
            before its trace could be persisted.
          $ref: "#/components/schemas/QuestionModelProfile"
        question_run_id:
          $ref: "#/components/schemas/OpaqueID"
        workspace_id:
          $ref: "#/components/schemas/OpaqueID"
        workspace_revision:
          type: integer
          minimum: 1
        conversation_id:
          description: Server-owned workspace-scoped conversation containing this turn.
          $ref: "#/components/schemas/OpaqueID"
        conversation_turn_id:
          description: Server-owned append-only turn bound to this Question Run.
          $ref: "#/components/schemas/OpaqueID"
        question:
          type: string
          minLength: 1
        answer_mode:
          type: string
          description: >-
            EXTRACTIVE (BYTE_EXACT_CITATION) selects authorized Evidence byte spans. TOOL_LOOP
            (ADDRESS_BOUND) calls the shared workspace knowledge tools with the selected
            workspace-available model profile and records address-bound citations and tool calls;
            source quotation checks do not by themselves prove answer completeness or semantic
            correctness. GENERATIVE (SEMANTIC_VERIFIER) sends selected Evidence and the question
            through the Model Gateway's interim adapter (GEN-2, ADR-0088). Its embedding-similarity
            verifier remains a ranking hint and never confirms a claim by itself: grounding requires
            a SOURCE_QUOTE whose span text is present in the stored extraction. An unverifiable
            GENERATIVE attempt completes its run in terminal INSUFFICIENT_EVIDENCE. The answer mode
            is fixed for the life of the run once created.
          enum: [EXTRACTIVE, GENERATIVE, TOOL_LOOP]
        verification_method:
          type: string
          enum: [BYTE_EXACT_CITATION, SEMANTIC_VERIFIER, ADDRESS_BOUND]
        grounding_status:
          type: string
          description: >-
            Answer-level R1 grounding state exposed identically by REST and MCP.
            CONFIRMED_BY_FRAGMENT ("confirmed by fragment") applies only when every claim has a
            bound SOURCE_QUOTE whose span text is present in the retained extraction. Anything else,
            including a paraphrase supported only by embedding similarity, is UNCONFIRMED
            ("unconfirmed"). The similarity threshold is not a proof input.
          enum: [CONFIRMED_BY_FRAGMENT, UNCONFIRMED]
          default: UNCONFIRMED
        status:
          type: string
          enum: [RUNNING, COMPLETED, INSUFFICIENT_EVIDENCE, FAILED, CANCELLED]
        corpus_status:
          type: string
          enum: [COMPLETE, PARTIAL]
        freshness:
          type: object
          additionalProperties: false
          required: [state]
          properties:
            state:
              type: string
              enum: [FRESH, STALE, FAILED, UNKNOWN, DISABLED]
            captured_at:
              type: string
              format: date-time
            last_successful_sync_at:
              type: string
              format: date-time
        started_at:
          type: string
          format: date-time
        completed_at:
          type: string
          format: date-time
        answer:
          type: string
        answer_hash:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        context_pack_hash:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        manifest_hash:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        manifest_status:
          type: string
          enum: [NOT_PUBLISHED, PUBLISHED]
        planning_status:
          type: string
          enum: [READY, UNKNOWN, CLARIFICATION_REQUIRED]
        planning_operation:
          type: string
          enum: [LOOKUP, EXPLAIN, COMPARE, AGGREGATE, AUDIT, CODE_TRACE, UNKNOWN, CLARIFY]
        planning_confidence:
          type: string
          enum: [NONE, LOW, MEDIUM, HIGH]
        plan_hash:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        clarification:
          type: string
          maxLength: 2048
        citations:
          type: array
          maxItems: 64
          items:
            $ref: "#/components/schemas/QuestionCitation"
        uncertainties:
          type: array
          maxItems: 32
          items:
            $ref: "#/components/schemas/QuestionUncertainty"
        conflicts:
          type: array
          maxItems: 32
          items:
            $ref: "#/components/schemas/QuestionConflict"
        failure_code:
          type: string
          pattern: '^[A-Z][A-Z0-9_]{2,63}$'
        answer_result:
          description: >-
            FIX-2 #1: structured counterpart of an AGGREGATE/LIST answer
            (populated only for a run answered by the snapshot reducer).
          $ref: "#/components/schemas/QuestionAnswerResult"
        understood:
          description: FIX-2 #2 ("Interpreted as"): populated only when Create spliced a bare period follow-up onto a prior turn's question.
          $ref: "#/components/schemas/QuestionUnderstood"
        searched:
          description: FIX-2 #6 ("where searched"), populated only when uncertainties includes INSUFFICIENT_EVIDENCE.
          type: array
          maxItems: 64
          items:
            $ref: "#/components/schemas/QuestionSearchedSource"
    QuestionToolLoop:
      type: object
      additionalProperties: false
      description: Encrypted run trace, reauthorized against every retrieved fragment before disclosure; provider reasoning is excluded.
      required: [profile, model, calls, messages, usage, stop_reason, all_claims_bound]
      properties:
        model_profile:
          $ref: "#/components/schemas/QuestionModelProfile"
        profile:
          type: object
          additionalProperties: false
          required: [id, max_turns, max_tool_calls, max_input_bytes, max_tool_result_bytes, max_output_tokens, timeout_seconds]
          properties:
            id: { type: string }
            model_artifact_sha256: { type: string, pattern: '^[0-9a-f]{64}$' }
            thinking_mode: { type: string, enum: [disabled, enabled] }
            sampling:
              type: object
              additionalProperties: false
              required: [temperature, top_p, top_k, min_p, presence_penalty, repeat_penalty, seed]
              properties:
                temperature: { type: number, minimum: 0, maximum: 2 }
                top_p: { type: number, exclusiveMinimum: 0, maximum: 1 }
                top_k: { type: integer, minimum: 1, maximum: 200 }
                min_p: { type: number, minimum: 0, maximum: 1 }
                presence_penalty: { type: number, minimum: 0, maximum: 2 }
                repeat_penalty: { type: number, exclusiveMinimum: 0, maximum: 2 }
                seed: { type: integer, minimum: 0, maximum: 2147483647 }
            max_turns: { type: integer, minimum: 2, maximum: 20 }
            max_tool_calls: { type: integer, minimum: 2, maximum: 40 }
            max_input_bytes: { type: integer, minimum: 8192, maximum: 524288 }
            max_tool_result_bytes: { type: integer, minimum: 1024, maximum: 262144 }
            max_output_tokens: { type: integer, minimum: 256, maximum: 16384 }
            timeout_seconds: { type: integer, minimum: 10, maximum: 300 }
        model: { type: string }
        stop_reason:
          type: string
          enum: [ANSWER, CLARIFICATION, TURN_LIMIT, TOOL_LIMIT, TRACE_LIMIT, TIME_LIMIT, CONTEXT_LIMIT, MODEL_UNAVAILABLE, OUTPUT_LIMIT, FORMAT_INVALID, CITATIONS_UNVERIFIED]
        all_claims_bound: { type: boolean }
        usage:
          type: object
          additionalProperties: false
          required: [prompt_tokens, completion_tokens, total_tokens]
          properties:
            prompt_tokens: { type: integer, minimum: 0 }
            completion_tokens: { type: integer, minimum: 0 }
            total_tokens: { type: integer, minimum: 0 }
        calls:
          type: array
          maxItems: 40
          items:
            type: object
            additionalProperties: false
            required: [id, name, arguments, arguments_hash, system, outcome, duration_ms, result]
            properties:
              id: { type: string }
              name: { type: string }
              arguments:
                type: object
                description: Tool-specific arguments, validated against the registered closed tool schema.
                additionalProperties: true
              arguments_hash: { type: string, pattern: '^sha256:[0-9a-f]{64}$' }
              system: { type: boolean }
              outcome: { type: string, enum: [SUCCEEDED, REFUSED] }
              duration_ms: { type: integer, minimum: 0 }
              result:
                type: object
                additionalProperties: false
                required: [text, is_error]
                properties:
                  text: { type: string }
                  is_error: { type: boolean }
                  structured:
                    type: object
                    description: Structured result of the registered MCP tool.
                    additionalProperties: true
        messages:
          type: array
          items:
            type: object
            additionalProperties: false
            required: [role, content]
            properties:
              role: { type: string, enum: [system, user, assistant, tool] }
              content: { type: string }
              tool_call_id: { type: string }
              tool_calls:
                type: array
                items:
                  type: object
                  additionalProperties: false
                  required: [id, type, function]
                  properties:
                    id: { type: string }
                    type: { type: string, enum: [function] }
                    function:
                      type: object
                      additionalProperties: false
                      required: [name, arguments]
                      properties:
                        name: { type: string }
                        arguments: { type: string }
    QuestionAnswerFilter:
      type: object
      additionalProperties: false
      required: [name, value]
      properties:
        name:
          type: string
          maxLength: 256
        value:
          type: string
          maxLength: 4096
    QuestionAnswerPeriod:
      type: object
      additionalProperties: false
      properties:
        from:
          type: string
        to:
          type: string
        label:
          type: string
          maxLength: 256
    QuestionAnswerSnapshot:
      type: object
      additionalProperties: false
      required: [row_count]
      properties:
        id:
          type: string
        captured_at:
          type: string
          format: date-time
        row_count:
          type: integer
          minimum: 0
    QuestionAnswerKey:
      type: object
      additionalProperties: false
      required: [key]
      properties:
        key:
          type: string
        fields:
          type: object
          additionalProperties:
            type: string
    QuestionAnswerResult:
      type: object
      additionalProperties: false
      required: [kind, operation, rule, snapshot, completeness]
      properties:
        kind:
          type: string
          description: CALCULATION is a deterministic snapshot reduction; LIVE_TABLE is a complete governed live read whose rows remain in the encrypted tool trace.
          enum: [CALCULATION, LIVE_TABLE]
        value:
          type: string
        unit:
          type: string
        operation:
          type: string
          enum: [COUNT, SUM, LIST, GOVERNED_READ]
        rule:
          type: string
        filters:
          type: array
          items:
            $ref: "#/components/schemas/QuestionAnswerFilter"
        period:
          $ref: "#/components/schemas/QuestionAnswerPeriod"
        timezone:
          type: string
        snapshot:
          $ref: "#/components/schemas/QuestionAnswerSnapshot"
        keys:
          type: array
          items:
            $ref: "#/components/schemas/QuestionAnswerKey"
        completeness:
          type: string
          enum: [COMPLETE, PARTIAL]
        # R2 Outcome 3 ("Semantics"/"Result"): the unified AnswerResult
        # fields, additive beside every R1 field above. All of them are
        # optional and a legacy answer that omits them still validates (the
        # required list is unchanged); a genuinely absent value stays absent
        # so the UI renders "no data" rather than an invented value.
        run_id:
          description: >-
            R2 Outcome 3: the Question Run this answer belongs to. The
            per-run identity is deliberately not folded into result_digest, so
            two runs of the same validated intent over the same snapshot tie
            out. Optional for a pre-R2 answer.
          type: string
          maxLength: 128
        intent:
          $ref: "#/components/schemas/QuestionAnswerIntent"
        rowset_ref:
          description: >-
            R2 Outcome 3: opaque reference to the rowset this answer was
            reduced from, when the run carries one. Never a raw SQL text.
          type: string
          maxLength: 256
        metric_version:
          description: >-
            R2 Outcome 3: version of the APPROVED MetricDefinition the
            validated intent executed against; absent for a run with no
            definition-backed intent.
          type: string
          maxLength: 64
        snapshot_id:
          description: >-
            R2 Outcome 3: stable snapshot identity the result digest is keyed
            by (never the per-run id).
          type: string
          maxLength: 128
        execution_id:
          description: >-
            R2 Outcome 3: stable execution identity the result digest is keyed
            by (never the per-run id).
          type: string
          maxLength: 128
        result_digest:
          description: >-
            For CALCULATION, SHA-256 of the canonical (RFC 8785/JCS)
            calculation projection and its stable identity. For LIVE_TABLE,
            the exact governedquery digest of {format, columns, row_count, rows}.
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        observation_window:
          $ref: "#/components/schemas/QuestionAnswerObservationWindow"
        receipt_digest:
          description: >-
            SHA-256 of the canonical (RFC 8785/JCS) server receipt projection.
            For LIVE_TABLE this binds the Question Run, operation, attempt,
            SQL hash, exposed-schema revision, database identity, exact result
            digest, row count, completeness and server execution window. It
            contains no SQL text or connection id.
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        freshness:
          $ref: "#/components/schemas/QuestionAnswerFreshness"
        evidence_refs:
          description: >-
            R2 Outcome 3: evidence identifiers the answer was grounded on.
          type: array
          items:
            $ref: "#/components/schemas/OpaqueID"
        audit_receipt:
          description: >-
            R2 Outcome 3: references to the R1 admission/outcome audit events
            for this run (audit identifiers, never audit content).
          type: array
          items:
            $ref: "#/components/schemas/OpaqueID"
    QuestionAnswerObservationWindow:
      type: object
      additionalProperties: false
      properties:
        basis:
          type: string
        started_at:
          type: string
          format: date-time
        completed_at:
          type: string
          format: date-time
    QuestionAnswerIntent:
      description: >-
        R2 Outcome 3: read-only projection of the QueryIntent the server validated against an
        APPROVED MetricDefinition before executing the run. Absent for every run that never carried
        a validated intent; in that case the answer panel shows "no data".
      type: object
      additionalProperties: false
      properties:
        metric_id:
          type: string
          maxLength: 128
        version:
          description: Monotonic per-id version of the APPROVED MetricDefinition.
          type: integer
          minimum: 1
        period:
          $ref: "#/components/schemas/QuestionAnswerPeriod"
        filters:
          type: array
          items:
            $ref: "#/components/schemas/QuestionAnswerFilter"
        output:
          type: string
          maxLength: 64
        as_of:
          description: Server-resolved as-of instant the validated intent used.
          type: string
          maxLength: 64
    QuestionAnswerFreshness:
      description: >-
        R2 Outcome 3: the immutable corpus-freshness snapshot the answer was
        produced against, optionally carried on AnswerResult. Same closed
        vocabulary as the run-level freshness field.
      type: object
      additionalProperties: false
      required: [state]
      properties:
        state:
          type: string
          enum: [FRESH, STALE, FAILED, UNKNOWN, DISABLED]
        captured_at:
          type: string
          format: date-time
        last_successful_sync_at:
          type: string
          format: date-time
    QuestionUnderstood:
      type: object
      additionalProperties: false
      properties:
        period:
          $ref: "#/components/schemas/QuestionAnswerPeriod"
        source:
          type: string
        inherited_from_turn_id:
          $ref: "#/components/schemas/OpaqueID"
        other_conditions:
          type: array
          items:
            type: string
    QuestionSearchedSource:
      type: object
      additionalProperties: false
      required: [source, result, message]
      properties:
        source:
          type: string
        result:
          type: string
          enum: [NOT_COVERED, NO_MATCHES, UNAVAILABLE]
        message:
          type: string
  responses:
    CSRFResponse:
      description: Derived CSRF proof.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
        X-Content-Type-Options:
          $ref: "#/components/headers/NoSniff"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/CSRFTokenResponse"
    WorkspaceResponse:
      description: Exact returned current or idempotently replayed workspace snapshot.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
        X-Content-Type-Options:
          $ref: "#/components/headers/NoSniff"
        ETag:
          $ref: "#/components/headers/ETag"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Workspace"
    WorkspaceListResponse:
      description: Workspaces visible to the authenticated principal.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
        X-Content-Type-Options:
          $ref: "#/components/headers/NoSniff"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/WorkspaceList"
    RequestInvalid:
      description: >-
        Malformed path, header, body, JSON, unsupported query, or missing
        required JSON member. FIX-7 #2: the response body's error.fields
        names the exact rejected field(s) (never their values), and the
        server logs one matching WARN (request_id, route, code, the same
        field names) so a request_id from a client is enough to diagnose the
        rejection from the server's own logs.
      $ref: "#/components/responses/ErrorResponse"
    Unauthenticated:
      description: Missing, invalid, expired, revoked, or tenant-mismatched session.
      $ref: "#/components/responses/ErrorResponse"
    CSRFRejected:
      description: Missing or invalid exact Origin / CSRF proof for unsafe operation.
      $ref: "#/components/responses/ErrorResponse"
    CreateForbiddenOrCSRFRejected:
      description: CSRF validation failed or create operation was denied.
      $ref: "#/components/responses/ErrorResponse"
    NotFound:
      description: Route absent, malformed canonical resource path, or resource unavailable to this access context.
      $ref: "#/components/responses/ErrorResponse"
    IdempotencyConflict:
      description: Idempotency-Key was previously used by this actor for a different command body.
      $ref: "#/components/responses/ErrorResponse"
    RevisionConflict:
      description: If-Match was syntactically valid but no longer equals current workspace configuration.
      $ref: "#/components/responses/ErrorResponse"
    PreconditionRequired:
      description: Mutation requires If-Match but it was absent.
      $ref: "#/components/responses/ErrorResponse"
    PayloadTooLarge:
      description: Body exceeds 32768 bytes.
      $ref: "#/components/responses/ErrorResponse"
    UnsupportedMediaType:
      description: JSON body route requires application/json; valid media-type parameters are allowed.
      $ref: "#/components/responses/ErrorResponse"
    ServiceUnavailable:
      description: Internal dependency failure; no implementation detail is disclosed.
      $ref: "#/components/responses/ErrorResponse"
    ErrorResponse:
      description: Content-free error response.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
        X-Content-Type-Options:
          $ref: "#/components/headers/NoSniff"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    SourceRegisterResponse:
      description: Idempotent registration projection; created=false on an exact replay.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
        X-Content-Type-Options:
          $ref: "#/components/headers/NoSniff"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/SourceRegisterResult"
    SourceActivateResponse:
      description: Durable job id of the placed SOURCE_SCOPE_SYNC work.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
        X-Content-Type-Options:
          $ref: "#/components/headers/NoSniff"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/SourceActivateResult"
    SourceListResponse:
      description: Current sync status of every source bound to the current workspace revision.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
        X-Content-Type-Options:
          $ref: "#/components/headers/NoSniff"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/SourceList"
    QuestionRunResponse:
      description: Immutable extractive Question Run projection with current-access-checked citations.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
        X-Content-Type-Options:
          $ref: "#/components/headers/NoSniff"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/QuestionRun"
    ConversationResponse:
      description: Current-access-checked conversation metadata and linked Question Runs.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
        X-Content-Type-Options:
          $ref: "#/components/headers/NoSniff"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Conversation"
    ConversationListResponse:
      description: Conversations visible to the authenticated principal in one workspace.
      headers:
        Cache-Control:
          $ref: "#/components/headers/NoStore"
        X-Request-ID:
          $ref: "#/components/headers/RequestID"
        X-Content-Type-Options:
          $ref: "#/components/headers/NoSniff"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ConversationList"
    ConversationIdempotencyConflict:
      description: Idempotency-Key was previously used by this actor for a different conversation archive command.
      $ref: "#/components/responses/ErrorResponse"
    QuestionIdempotencyConflict:
      description: Idempotency-Key was previously used by this actor for a different question.
      $ref: "#/components/responses/ErrorResponse"
    SourceConflict:
      description: The derived lineage already exists with a different configuration.
      $ref: "#/components/responses/ErrorResponse"
    SourceUnavailable:
      description: No verified FOLDER capability profile is provisioned yet.
      $ref: "#/components/responses/ErrorResponse"
    GovernedQueryUnavailable:
      description: Governed query is unavailable for this connection - one opaque code for a disabled flag, a missing exposed schema and an unreachable dedicated role (ADR-0089 section 6).
      $ref: "#/components/responses/ErrorResponse"
    GovernedQueryFailed:
      description: The model could not compose a statement the exposed schema and the read-only role accept, or its execution was refused by the bounds.
      $ref: "#/components/responses/ErrorResponse"
